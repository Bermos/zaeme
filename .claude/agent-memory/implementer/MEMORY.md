# Implementer memory

What implementers of Bermos/zaeme learn the hard way. One line per lesson, dated,
under the heading it belongs to. Merge duplicates; do not drop.

The entries dated 2026-09-14 are the seed, read out of `CLAUDE.md`, the test suite and
the open issues when this file was created rather than out of a run; the rest is earned.

## Checks that prove less than they look

- 2026-09-16 (#29): A RULE WRITTEN OVER HANDLER SOURCE CANNOT SEE A PAYLOAD THE HANDLER DOES NOT NAME. "Every human handler that answers with a budget signs its receipts" was first written as a grep of each handler for the word `budget` — and passed while `GET /api/invites/{token}` served every receipt UNSIGNED, because that handler is one line (`return getInvitePage(token)`) and the budget is nested three levels inside what it returns. SSR renders the guest page off exactly that route. The honest form computes the producer set FROM THE DOMAIN and closes over it: start at `loadBudget`, add any exported function whose body calls something already in the set, then require every handler calling one to sign. It found 13 where the grep found 12. AND THE CLOSURE ITSELF HAD TWO BLIND SPOTS OF THE SAME SHAPE (#73 review), both executed: the producer regex matched `export function` only, so an `export const f = async () => loadBudget(id)` producer plus an unsigned handler passed all 61 assertions; and the handler filter listed guest+host+account while `adminHandlers` was built three lines above it, so an unsigned admin budget route passed too. A rule computed from a regex has that regex's blind spots, and a filter over "the surfaces I was thinking about" is a list wearing a closure's clothes — enumerate the spellings and the surfaces, and assert the directory you walk is as flat as `readdirSync` assumes.
- 2026-09-16 (#29): AN EXPLICIT `.select({...})` IN THE DOMAIN IS A THIRD PLACE A NEW COLUMN HAS TO BE ADDED, after the schema and the `/api/v1` projection. `listMedia` (`server/domain/events-data.ts`) lists its columns by hand, so `expenseId` reached the OpenAPI document, `v1-shapes.mediaItem` and the type checker while the query never selected it — `null` for every item, lint/typecheck/vitest all green. The only thing that caught it was a smoke check asserting the value the machine surface actually SERVES. Grep `select({` in `server/domain` for the table you touched. Same family as `events_expense.currency`, which `loadBudget` returned from `expenses[0]` while `computeBalances` summed cents across currencies (#25): A COLUMN EXISTING IS NOT THE FEATURE EXISTING — read the function that consumes it before believing a schema comment. AND IT HAPPENED AGAIN IN #35, same function, same shade of green: `ticket` reached the OpenAPI document, `v1-shapes.mediaItem` and the type checker while `listMedia` never joined `events_ticket_detail`, so the machine surface reported `null` on a ticket with a seat written on it. `listMediaForPlanner` (server/domain/media.ts) and `listMedia` (server/domain/events-data.ts) are TWO planner media reads that share no code; a smoke check that writes and then re-reads through the OTHER surface is the only thing that has ever caught either one.
- 2026-09-16 (#29): "NOTHING CHANGED" IS HONESTLY ASSERTED BY COMPARING A RESPONSE TO ITSELF, not to a literal. `money_of <body> <title>` in `scripts/api-smoke.sh` prints every figure and label an entry states about its own money; the pin's rule is `equals "$(money_of "$AFTER" X)" "$(money_of "$BEFORE" X)"`, with no expected value written down at all. A literal there is a constant standing in for "unchanged" and is satisfied by an implementation that rewrites the row to the same defaults. It is also unusable WITH a literal: it joins with `" "` and `[null].join(" ")` is an EMPTY string, so a row with no stated pair expects two consecutive spaces nobody reads correctly twice (#28) — for a by-hand figure, needle the entry's JSON with `contains` instead. AND IT NEEDS BOTH DIRECTIONS: the mutation that made the pin write `fx_rate_source = 'manual'` reddened only the `fetched` fixture, because the `manual` one was already `manual` — a relabel check with one fixture proves one of the two lies it exists to catch.
- 2026-09-16 (#29): A 404 ON A MADE-UP ID DOES NOT PROVE SCOPING. "A photo from another trip cannot be pinned" needs a REAL id from a REAL other event: drop the `eventId` half of the lookup and the made-up id still 404s while a stranger's photograph becomes pinnable. Mint the second event.
- 2026-09-16 (#29): PIN A FORM RULE ON THE ENDPOINT AND THE STATE, NEVER ON A WORD, when the word is overloaded. `receipt` means two things in `BudgetCard.vue` — the paper somebody photographed, and the foreign-currency figure an expense was recorded FROM — and `saveExpense` discusses the second at length. `expect(save).not.toMatch(/receipt/i)` fails on a comment and would pass on a body field called `photo`; `/\/receipt`/` and the ref names do not. Same family as #32's two grep-the-file assertions, which their own doc comments satisfied: a rule a COMMENT can satisfy is not a rule — slice the function, or match the call site.
- 2026-09-16 (#59 review): AN ASSERTION WITH TWO WAYS TO PASS PROVES NEITHER. The block written to catch "was this converted" reading the currency codes instead of the rate used a two-way split, whose recompute leaves a cent on `Rounding` — so `approximate` came back true through its OTHER disjunct and the mutation that restored the broken rule went GREEN on it. One participant makes the residual structurally zero and leaves the rate as the only thing that can answer. Enumerate every disjunct a check can pass through and close all but the one under test.
- 2026-09-16 (#59 review): A ROUND-TRIP FIXTURE PICKED AS A NEAR-INVERSE PROVES REVERSIBILITY OF NOTHING. `1.0865432109` then `0.9203456789` lands every figure back on itself and the comment beside it admitted the exactness was "a property of these numbers" — so the only executed proof of "a manual figure survives" was tuned to succeed, while real non-inverse rates moved a CHF 234.00 hotel to 235.18 and compounded. When a check's success depends on the constants, say which property is being tested and pick constants that do NOT flatter it.
- 2026-09-16 (#59 review): THE `upgrade` JOB CANNOT DISTINGUISH TWO BACKFILL SOURCES THE PREVIOUS RELEASE'S API CANNOT MAKE DIVERGE. "Take the event's currency from its expenses" and "take it from the instance setting" differ only where they already disagree, and #25's 409 made that state unreachable — 0 of 13 events differed in the database that job builds. A mutation verified against a hand-built trimmed-journal fixture is a claim about your terminal; say so in the table rather than crediting CI. What the job CAN see is the other half, so stop skipping the events with no expenses.
- 2026-09-16 (#59): A MONEY FIXTURE HAS TO BE ECONOMICALLY COHERENT, not merely lopsided. The first one had a EUR 245.25 bill costing CHF 208.01, which is a rate no bank charges in that direction (CHF is worth MORE than EUR), and it made the recompute's own rule look wrong: the payer came out owed less than the receipt. Sanity-check the direction of every rate in a fixture against reality before pinning it — the arithmetic will happily prove nonsense.
- 2026-09-16 (#59): adding ONE field to a budget response breaks every `contains` needle that spans it. `'"currency":"CHF","totalCents":44362'` stops matching the moment `approximate` lands between them, in three pre-existing checks. Expect that, and grep the smoke script for adjacent-field needles before believing a new field is additive. They are also matched against RAW JSON, so a message quoting a name (`has no category called "Sherpas"`) arrives with the quotes ESCAPED and a needle carrying bare quotes never matches — needle the unquoted prefix.
- 2026-09-14 (#63 review): A CHECK THAT RECOMPUTES THE SERVER'S OWN ANSWER FROM THE SERVER'S OWN RESPONSE CANNOT FAIL. `ci-upgrade-check` summed the category debits out of `budget.expenses[].lines` and compared them to `totalCents` — which `computeTotalCents` produces by summing exactly those lines — so it restated what it was checking, passed 0 == 0 under the very mutation it looked like it covered, and still counted towards an anti-vacuity floor. The honest form is a figure the CHECK chose: write an expense of 3000 and require the total to move by 3000.
- 2026-09-14 (#63 review): TWO FLOORS THAT DISAGREE FAIL AT THE WRONG END. A producer that accepted "at least 2 expenses" and a consumer that demanded "at least 24 assertions" over a canary worth 20 meant a future cleanup in `api-smoke.sh` would pass the snapshot and then exit 1 with "proved almost nothing" AFTER every check it could run had passed. Count the floor from the code, write the arithmetic into the comment, execute it (a snapshot trimmed to the canary alone), and make the producer refuse anything that would make one of the counted assertions unreachable.
- 2026-09-14 (#63): AN EMPTY DATABASE MAKES EVERY BACKFILL A NO-OP, and that is what CI proved until now. Executed both ways: delete the `UPDATE` that points existing share rows at their member account in `0006_red_redwing.sql` and the chain still applies to an empty Postgres, seeds, boots and passes the whole smoke suite (266 of 266 locally) — against a database the previous release filled it dies on `ALTER COLUMN account_id SET NOT NULL`, "contains null values". A mutation that is green in `api` and red in `upgrade` is the shape to expect from anything a migration does to rows.
- 2026-09-14 (#63): the app HEALS some migration damage on read, which hides it. `ensureEventAccountsWithin` recreates `Uncategorised` and `Rounding` the first time a budget is loaded, so a migration that seeded no chart of accounts for existing events still reads 200 and still balances — the money is simply missing from the trip total. What exposes it is a WRITE naming a category the migration was responsible for creating (`category: 'Food'` → 422 "This event has no category called Food"). Assert on a named write, not only on a read coming back.
- 2026-09-14: A CHECK THAT PASSES WHEN THE THING IS BROKEN IS WORSE THAN NO CHECK. Three shipped in one branch (#57 review): a settings PATCH asserting `"configured":true` rather than the value written (an implementation that discarded its input passed all 168); a budget-currency assertion on a trip whose FIRST expense happened to be in the base (reverting the fix stayed green); and a 422-on-an-unfetchable-currency as the only FX-client coverage (a moved host, a renamed field and a real 404 are all the same 422). Each needed the same remedy: make the check depend on the value, and on a fixture where the wrong answer differs from the right one. A REFUSAL check aimed at a fixture an EARLIER check has since changed is the same bug arriving sideways: #27's "a mixed even split is refused on /api/me" pointed at an entry a previous check had given an explicit even split, so it had stopped being the refusable case at all. Mint the fixture a refusal needs inside the block that needs it.
- 2026-09-14: `scripts/api-smoke.sh` RE-RUNS against a database that already has the last run's rows, so a new check must be idempotent: mint your own event, and never assume the instance is in its default state (a check that only passes on a fresh Postgres passes CI and fails the first person who runs it twice). A precondition a check needs must be CREATED by the script too, never assumed — otherwise it proves the refusal on a re-run and the value on a fresh database, which is the worst of both. A cache assertion is the sharpest case: without `$SUFFIX` in the query the second run is served from the first run's cache and the check proves the opposite of what it claims.
- 2026-09-14: `pnpm test` is vitest, and **vitest does not typecheck**. A type error in a Vue file or a server handler passes the tests and fails `pnpm typecheck` (`nuxt typecheck`, vue-tsc) — which CI runs as its own step. Run both.
- 2026-09-14: anything whose behaviour depends on a real query — an ordering, a correlated sub-select, a cascade, a unique constraint — is proved by no vitest test. Since #14 the `api` job boots `.output/server/index.mjs` against Postgres and runs the whole `smoke:api` suite, poster round trip and both session halves of the boundary included, so that is where such an assertion belongs; `smoke:passkey` still runs nowhere but a terminal. `pnpm smoke:passkey` is the exception that stays outside it: it drives the real WebAuthn ceremony with a software authenticator (`scripts/webauthn-authenticator.mjs`) and runs nowhere but a terminal, so a green pipeline still says nothing about that surface.
- 2026-09-14: `pnpm smoke:api` needs no dev server and no `pnpm dev`: the BUILT output boots with `DATABASE_URL`, `PORT`, `BASE_URL`, `BETTER_AUTH_SECRET`, `ZAEME_SERVICE_TOKEN` and `ZAEME_ENTERPRISE_OWNER_ID` and nothing else. Booting the build is the only check that catches a module that resolves at build time and not at boot — `pnpm build` exiting 0 does not.
- 2026-09-14: `pnpm lint` is `eslint .` through `withNuxt()` and applies **no markdown rules**, and nothing in CI reads a comment either — a prose-only change passes lint, typecheck, vitest and the `api` job without one assertion touching it. The only check on a doc or a comment is somebody re-reading the code each claim describes, one clause at a time, and saying in the PR which file proved which clause (#54).
- 2026-09-14: A COMMENT THAT IS TRUE OF THREE CASES OUT OF FOUR IS A FALSE COMMENT, and nothing in CI reads it. #26's `(splitMode, weight)` pair was documented in four places as existing "so an edit can re-split without re-typing" — true for `percentage`, `weight` and `exact`, and FALSE for `even`, which honours explicit per-person amounts while recording nothing about which participants were pinned. Enumerate the modes/branches against the claim before writing it, and say what is NOT recoverable in the same breath. The same class, found in #36's review: two route comments said the reply exists "so the card re-renders from the answer" while the ONE client in the same diff discarded every response and re-fetched — it had to, because a domain view carries a `storageKey` and the card needs a signed `url`. Before writing what a payload is FOR, open its only caller; the behaviour was right and the sentence was committed fiction. AND A BRANCH THAT CHANGES WHO MAY READ WHAT FALSIFIES THE RECORDED REASONS FOR NEIGHBOURING EXCLUSIONS, and those then read as obsolete rather than as stale. `RECEIPT_TYPES` excluded `ticket` because "a ticket is per-person and pinning one would publish it to the whole trip"; after #37 the whole trip reads it anyway, so the exclusion needed a NEW stated reason (a receipt is what you were CHARGED, a ticket what you were ISSUED) or the next reader would correctly conclude the premise was gone and widen the list. Same shape for the `unassigned` badge, whose EXPLANATION inverted while its LABEL stayed accurate. Grep for the old rule's WORDING, not just its code — five sentences in five files, and nothing in CI reads one.
- 2026-09-14: An OPEN issue is not an unimplemented issue. The host half of #8 had already landed on `main` (bee78a3) with a comment on the issue describing it, and the issue stayed open for the admin half. `git ls-tree -r origin/main -- <path>` before writing anything an issue says is missing.
- 2026-09-16 (#37): AN ACCEPTANCE CRITERION CAN BE A CLAIM ABOUT PRACTICE WEARING A CODE INVARIANT'S CLOTHES, and "verify that holds rather than assuming it" is the issue telling you so. #37's was "a concert exposes no uploaded tickets, because it has none" — true today and enforced NOWHERE: `media/presign` takes `type: ticket` for every event type, nothing in `registerMediaUpload` reads `event.type`, and no invite mint refuses a concert. Go and read the gate the criterion names before pinning it; the property that actually held was different and stronger (NO PUBLIC SURFACE CARRIES MEDIA AT ALL — `PublicEventPage` has no gallery, documents or tickets and there is no public media route), so pin that and report the gap.
- 2026-09-14: `updated_at` moves on its own — the events schema puts `.$onUpdate(() => new Date())` on those columns, so a hand-built `updates` object must NOT set it and does not need to. Its precision is MILLISECONDS while the column stores microseconds, so `updated_at` as a lost-update fingerprint cannot tell apart two writes landing in the same millisecond — fine as a second line behind a row lock (#27 takes the event lock first), never a substitute for one.
- 2026-09-14: `GET /api/admin/audit` answers `{entries, summary}` and `summariseAudit` IGNORES the query string — `summary.bySurface` always names every surface and `summary.byActor` every kind, so a filter matching NOTHING still returns them beside `entries: []`. A smoke needle like `'"surface":"me"'` or `'"actorKind":"participant"'` is therefore satisfied by the summary and proves nothing (#51 review: two of six new checks were vacuous, and one of them was the check the PR body cited as proving its decision). Needle on `"path"` or `"actorLabel"`, which appear in no summary. The same trap waits wherever a filtered list ships beside unfiltered aggregates. A vacuous needle also DISARMS `audit_await`: the poll returns on its first read because the summary already matches, so the write-then-read race stays wide open for the `contains` after it.
- 2026-09-14: MUTATION-TEST EVERY NEW SMOKE CHECK BEFORE CLAIMING IT PROVES ANYTHING. Break the behaviour on purpose (patch the middleware, rebuild, re-run) and confirm the check you are relying on goes red, one mutation per claim in the PR body. #51 shipped a body asserting that labelling the whole surface one way "would have broken this one" — it broke a different one, and the cited check passed with `entries: []`. COUNT THE MUTATIONS BY WHAT EACH ONE REACHES — one per claim, because a feature with a dispatch and an arithmetic needs both (#26: "record the mode and ignore it" reddened 21 of 29 checks, "split evenly among the non-zero weights" reddened a different 10) and because the SPREAD is itself the finding (#61: posting every line to `Uncategorised` reddened 13 smoke checks and ZERO vitest tests).
- 2026-09-14: A MODE'S HAPPY PATH CAN COINCIDE WITH THE DEFAULT'S. `exact` 6000/3000/1000 of 100.00 is what `even` gives for the same input (explicit amounts are honoured there too), so that assertion stayed green under "ignore the mode" — the only check that distinguishes `exact` is that it refuses a participant with NO amount. Mutation testing is what showed which of the two was load-bearing; do not claim a value check proves a mode until it has gone red. Same species: `totalCents` is the expense amount whatever the shares are, so it passes with the split reversed — assert a BALANCE the default gets backwards (seven people and 100.00 gives the four leftover cents to the FIRST four).
- 2026-09-14..16: PROVE A FEATURE ON THE SURFACE THE PRODUCT USES, AND WITH THE BODY THE PRODUCT SENDS — two different failures, a release apart. #26 shipped 29 smoke checks that ALL posted to `/api/v1` with the service token while `BudgetCard.vue` posts to `/api/me` and `/api/host`: narrowing those two human schemas broke every percentage and weight split a PERSON could make and left vitest, lint, typecheck and the smoke floor green. #27 then covered all three surfaces — and still missed, because every check sent a body `scripts/api-smoke.sh` had composed while the FORM composed a different one (`splitMode` + `participants` on every correction, the prefilled rate on every correction), so correcting the TITLE of a hand-pinned `even` expense re-split it evenly and moved money between friends, and correcting anything on a foreign row relabelled it as checked against a bank statement. Both survived a full green pipeline. `grep -c 'api/me\|api/host'` your new block, then copy the body the form builds field for field — and know that even then it pins what the SERVER does with that shape, never the form's choice to send it.
- 2026-09-14: On a human surface assert **422, not just "not 200"**: a refusal that arrives as 400 came from the zod schema, which never let the mode through, and a refusal that arrives as 422 came from the domain, having understood it. The two are the same red in a `check … 4xx` and opposite findings.
- 2026-09-14: A `contains` cannot do arithmetic, and a double-entry invariant is arithmetic. `scripts/api-smoke.sh` now has `ledger_imbalance`/`entry_lines`/`account_id`, three one-line `node -e` helpers over the response body — every machine that can build this app has node, and the alternative (a needle per figure) passes on a budget whose entries do not balance. Make such a helper return a SENTINEL (`no-expenses`, `unparseable`, `no-such-entry`) rather than `0` when it finds nothing, or a dead server reads as a clean bill of health — and two empty strings compare EQUAL, so an extractor that finds nothing makes "this value did not change" pass against a corpse. A `grep -o '"startsAt":"…"'` over an aggregate is the other half of that trap: an event's own start, a poll option's and a timeline item's share the key, so the answer depends on which surface is read (#31 needs `tl_start <body> <title>`, which names the row).
- 2026-09-14: a 2880-case property sweep goes VACUOUS WITHOUT FAILING when the code stops calling the function it sweeps. #61 moved conversion out of `apportionCents` and `test/expense-splits.test.ts`'s sweep kept passing over a call the write path no longer made — 184 green, a heading renamed, and no signal at all that the invariant was gone. When a change removes a call, grep the test suite for the callee before believing the suite still covers it. And the events domain (~3,400 LOC) still has almost no unit coverage (#9): a change there is as good as the test you write for it.
- 2026-09-15: a DEFAULT that differs BY SURFACE is its own check, twice. `isPlanned` is true when the host says nothing and false when the invite link does; one shared default passes exactly one of that pair, so assert both or the pair proves nothing. The same shape will recur wherever two credentials write one table.
- 2026-09-15: A FAKE CLOCK WHOSE `sleep` ADVANCES A SHARED `t` CANNOT TEST A RATE LIMITER. It serialises concurrent callers, so the broken shape (read the clock, sleep, and only THEN reserve the slot — which lets a whole burst through at once) produces exactly the same numbers as the correct one. Model a sleep as a pending wake-up, release the earliest and move time once; then the wrong implementation lets all three through at t0 and the right one spreads them. Flush microtasks BEFORE the first wake-up or the caller who waited 0 records the clock as it stands after it.
- 2026-09-15: A SMOKE CHECK ON A THIRD-PARTY READ CANNOT BE A `contains` ON WHAT CAME BACK — and it cannot be skipped either, because the `api` job fails on ANY `skip` line. What works is asserting the SHAPE the feature promises in both cases (a `node -e` helper that validates the results when they arrive and the degrade envelope when they do not), plus a derived expectation for anything stateful: `if the first answer was ok then the second must be cached, else it must not be`, which runs and can fail whichever way the afternoon went. THE ONE CHECK IN THIS SUITE THAT BREAKS THAT RULE ON PURPOSE is `a live rate actually comes back`, the single real call to frankfurter (`GET /api/me/fx/rate?from=EUR`), and it therefore FLAKES: it answered `{"rate":null,"asOf":null}` on a runner during #31 while passing twice on the agent box minutes apart. A red `api` job whose only failure is that line is the internet, not your diff — confirm it is the only one, say so, and re-run once.
- 2026-09-15: Unicode escapes written into a test file through a tool can arrive as the CHARACTERS they name — `'Zu\u0308rich'` became the composed word, quietly turning an NFC test into a tautology. Bind the two forms to consts and assert they differ before asserting they normalise the same.
- 2026-09-15: TESTING BOTH HALVES OF A GATE IS NOT TESTING THEIR INTERACTION. #32's cooldown was checked only BEFORE the limiter's sleep, so `block()` never reached the two or three callers already asleep — one 429 answered with three more requests over three seconds, which is the exact "retry into a refusal" the module's own comment forbade. The cooldown test used `sleep: async () => {}` and two SEQUENTIAL calls, so nobody was ever mid-sleep when the block fired. Any state a waiter can miss needs re-reading after the wait, and a test where the two events overlap. The same gate also mapped every refusal to one word ("our queue is too long") including the five minutes after a provider said stop, which is the opposite fix: make a gate answer an ENUM and assert the REASON, not just the status.
- 2026-09-16: `ensureMemberAccountsWithin` keys `wanted` on the EMAIL and the LAST name written wins, and both `addExpense` and #27's edit pass `[payer, ...shares]` — so a payer who is also in the split is named by the SPLIT, not by `paidByName`. A smoke check asserting a payer credit by NAME fails on the first fixture where the two differ; assert on the address, which is the identity everywhere in this domain.
- 2026-09-16: AN ASSERTION THAT ENCODES A PROPERTY OF WHICH RELEASE HAPPENS TO BE THE BASE GOES STALE THE MOMENT THE BASE MOVES, and it fails for every branch afterwards regardless of their diffs. `ci-upgrade-check.mjs` asserted `fxRateSource === 'fetched'` for every row the previous release wrote; it passed on #59's own branch only because the release it replaced had no such column, and once #59 was the base the previous release legitimately wrote `manual` rows (the smoke suite writes over a hundred) and the `upgrade` job went red on #27 for somebody else's reasons — its own canary among the casualties. The fix is always the same shape: assert against what the SNAPSHOT recorded, not against a constant, and keep the constant only for the case where the snapshot has nothing to compare (a base predating the column), where it is the right assertion. When you find one, grep the whole file for siblings — and read the label and the figure behind it as ONE fact, because comparing only the label passes a migration that invents a stated amount beside an untouched one.
- 2026-09-16 (#28, and AGAIN in its review): A SMOKE HELPER THAT FINDS AN ENTRY BY TITLE FINDS THE FIRST ONE, and a settlement's title is GENERATED ("Ben → Ana"), so two payments between one pair are two entries with one name. It cost the same block twice: `expense_id` returned the older row, the DELETE removed it, and the checks after it failed for reasons unrelated to what they test — the second time while the sequence-it-carefully fix from the first time was in the file. Sequencing is not the fix; TAKE THE ID FROM THE WRITE THAT MADE IT. Every budget write answers `budget.expenseId` (the domain spreads it into the budget), so `json_field "$RESP" budget.expenseId` is exactly the row just written, whatever it is called. And any read-back assertion about "the refusal wrote nothing" must be a FIGURE (a total, a balance) rather than a by-title lookup, for the same reason.
- 2026-09-16 (#28): UNIT TESTS THAT BUILD THE LEDGER LINES THEMSELVES CANNOT SEE THE WRITE PATH GETTING THEM BACKWARDS. Swapping payer and recipient in `recordSettlement` reddened 15 smoke checks and ZERO of twelve brand-new vitest tests, because those tests pass the pair in themselves — so the only thing that noticed needed a built server, a live Postgres and two cookies. `vi.mock('../server/domain/expenses', importOriginal)` with `addExpense` returning its own arguments pins the TRANSLATION (who is the payer, who is the share, which destination) in 300ms and goes red on exactly that mutation.
- 2026-09-16 (#31): A TIMEZONE ASSERTION IS VACUOUS IN THE ZONE IT ASSUMES. CI runs in UTC and `formatInZone('…T08:14:00Z', 'Europe/Lisbon')` is `09:14` in July — which is also what an implementation IGNORING the zone answers, because Lisbon is UTC+1 that month, so every assertion passes against code that dropped the feature. `test/event-timezone.test.ts` sets `process.env.TZ = 'Pacific/Chatham'` before its first import (Node reads it lazily for both `Date` and `Intl`; vitest isolates each file in its own fork, and it is restored in `afterAll` anyway), so the ambient rendering is `20:59` and no expectation can coincide with either the stored instant or the ambient reading. Assert the ambient one too, as a control: it is what says the file is still standing where it thinks it is.
- 2026-09-16 (#31 review): ANY ELEMENT INSERTED BETWEEN A `v-if` AND ITS `v-else-if` BREAKS THE CHAIN SILENTLY. A new card dropped between `<div v-if="suggestions.length">` and `<p v-else-if="searchState === 'unavailable'">` left the first branch terminating in a comment node and re-parented both `v-else-if`s onto the NEW condition — so a search returning three results rendered them and said "No matches" directly underneath, with lint, typecheck and every test green. Vue compiles this happily and nothing in CI renders that card. Put new siblings AFTER the whole chain, and check it the way the review did: `compileTemplate` the SFC (`@vue/compiler-sfc` is in the pnpm store) and read which condition each branch hangs off.
- 2026-09-16 (#31): JSON CARRIES AN INSTANT, NOT A RENDERING, so a whole feature about how a time is DISPLAYED sat behind 26 green smoke checks with the formatting dropped — mutation-tested: `formatInZone` ignoring its `timeZone` reddened 6 vitest tests and ZERO smoke checks until three more were added that fetch the SSR'd invite page as HTML. If the feature is a rendering, something has to read the rendered page — and A SMOKE SUITE WATCHES THE WIRE, so a defect that lives in the RENDERER survives it whatever the server sends: the review found the zone offer keeping `.slice(0, 4)` of the twenty-nine zones the API correctly returned, and the check asserting the response would have passed. Where a component decides something, move the decision into a pure function the unit tests can call (`zonesToOffer`) rather than asserting the payload it was given. AND THE FIXTURE HAS TO BE ABLE TO EXPRESS THE FAILURE: every geocoding fixture here was Portuguese, and Portugal has exactly three zones, so `slice(0, 4)` was invisible to the stub, to both test files and to every check; the whole `DatePoll` component was likewise executed by nothing because the smoke fixture was a trip with a timeline item and never a party with a poll. Two traps in reading a page: Nuxt serialises the whole payload into `__NUXT_DATA__` on the same document, so the raw `2027-07-01T08:14:00.000Z` is in the HTML by construction and a bare `excludes '08:14'` fails against a perfectly correct render (anchor on element boundaries, `>09:14<`); and the fixture was called "The 09:14 to Lisbon", so the needle matched its own TITLE and passed with the rendering gone (renamed to "The early ferry west"). Same species as `totalCents` and the audit summary — ask what else in the body matches your needle.
- 2026-09-16: A PREFILLED FIELD SENT BACK IS NOT A STATEMENT ANYBODY MADE. The expense form fetches a mid-market rate INTO the rate box as a convenience and submitted it verbatim, so `fx_rate_source` — the column whose whole meaning is "a person checked this against a bank statement", and which a currency recompute trusts — read `manual` on every foreign expense ever recorded through the product. Compare against what the form PUT there (`fxRatePrefilled`) rather than against empty, and send the field only when it differs or the currency moved.
- 2026-09-16: MUTATION-TESTED #35, six mutations, and the spread is again the finding: rendering the validity in the reader's clock reddened 6 vitest tests and ZERO smoke checks (the ticket list is fetched client-side, so no SSR'd HTML carries it — the #31 hole in a second place); a write that discarded its input reddened 14 smoke checks and zero vitest; folding an emptied detail row into "no detail row" reddened exactly 2; showing every ticket to every guest reddened 3; `.min(1)` on one field reddened 1 vitest + 3 smoke; dropping the `eventId` from the media lookup reddened 2 — one as a 500, because the composite FK refuses the cross-event write at the database.
- 2026-09-16: A HELPER THAT FOLDS TWO ABSENCES TOGETHER CANNOT TEST THE DIFFERENCE. `ticket_field` in `scripts/api-smoke.sh` answers three distinct sentinels — `no-such-item` (the viewer cannot see the row), `no-detail` (no child row) and `null` (a child row with a blank field) — and the whole of #35's "an empty detail is a state, not an absence" rests on the last two being different strings. Write the sentinel set before the checks. Its sibling `media_field` has a narrower version of the same defect: it resolves `items.media ?? items.gallery ?? []`, so on the invite surface (`{gallery, documents, tickets}`) it looks in the GALLERY and answers `no-such-item` for every ticket.
- 2026-09-16 (#35 review): MOVING A DECISION INTO A PURE FUNCTION MAKES THE RULE TESTABLE AND LEAVES ITS ARGUMENT PINNED BY NOTHING. `ticketDetailLines(detail, timezone)` had 18 tests over the rule; the `timezone` prop feeding it was OPTIONAL, and `formatInZone` falls back to the ambient zone for `undefined` by design — so deleting the one `:timezone=` binding on the guest page gave eslint clean, typecheck clean, 426 vitest passed, 731 smoke passed, with every ticket rendered in the reader's clock. Make a prop that carries a zone, a currency or a locale REQUIRED (`x: string | null`, never `x?:`) so vue-tsc refuses the omission, and assert the requirement in `test/api-boundary.test.ts` too — reverting `x:` to `x?:` is one character and typecheck goes quiet again. #37 then pinned the ARGUMENT structurally, and two things make that test real rather than decorative: read the `<template>` (`/<template>([\s\S]*)<\/template>/`) and not the whole source, because the prose above a binding names the binding too and a comment explaining what should be there passes a source-wide `toMatch`; and COUNT what a walk of `app/` found (`expect(callSites).toBe(2)`), because a list of "call sites missing the binding" is empty both when every one binds it and when the walk found none at all. AND THE SAME HOLE REOPENS ONE LEVEL DOWN — A STRUCTURAL TEST THAT READS ONLY `<template>` LEAVES EVERY EXPRESSION IN `<script setup>` PINNED BY NOTHING, and that is where the #35 "argument, not body" lesson reopens — closed for DELETING a binding, wide open for WIRING one wrong. Four one-word mutations survived eslint, `nuxt typecheck` and 446 tests with the card still rendering: the default-scope literal flipped (deleting an acceptance criterion outright), the guest page binding `:viewer-email="null"`, `identified: true`, and `ticketsInScope(…, 'all')` making both buttons labels while `v-for="t in shownTickets"` still matched. FIX IT AT THE ROOT: export the default as a VALUE a test executes; fold the several derivations into ONE call pinned verbatim, deriving the flags inside the rule rather than at the call site; and assert a binding's VALUE, not its presence — `:viewer-email="null"` is correct beside `:tickets="[]"` and a regression beside real tickets, so the rule is about the PAIR. Strip comments before asserting over a script: the prose explaining why a line is not `'mine'` has to quote `'mine'`.
- 2026-09-16 (#35 review): A `datetime-local` DRAFT SEEDED FROM ONE ZONE AND SAVED AGAINST ANOTHER MOVES THE INSTANT. `HostMediaCard` watched its own `useFetch` and not `props.timezone`, so a zone edited further up `host/[slug].vue` moved the prop while the drafts kept the old reading: 22:59Z seeded as 23:59 Lisbon, saved after a switch to America/New_York as 03:59Z next morning, on a field nobody touched, for the page's whole lifetime. `rezoneInputValue(local, from, to)` (shared/utils/timezone.ts) is the conversion — keeps the instant, changes the reading, so an unsaved edit survives. Any card that seeds wall-clock fields EAGERLY must watch the zone; converting at edit time (`HostTimelineCard`) has a window of one click instead.
- 2026-09-16 (#36): THE SMOKE SUITE DELETES ITS OWN FIXTURES, so "the previous release leaves rows like this behind" is a claim to CHECK before a migration is verified against them. #35's block uploads a ticket, assigns it and then DELETES it, so #36's backfill would have been proved against an empty set with the `upgrade` job green. Remedy: mint your OWN canary through the previous release — `ci-upgrade-check.mjs` can use its HOST surface with the cookie `ci-smoke-setup.mjs sign-in` exported, which is the only way to create a ticket at all (`/api/v1` has no upload route) — and have the new smoke block LEAVE its fixture in place for next time. Give that canary a route fallback (old verb, then new) or it aborts every branch cut after the change lands. Watch the shapes on the way: `GET /api/v1/events/{slug}/rsvps` answers `{rsvps, summary}` and not a bare array — the one list endpoint on that surface that wraps itself, and `(rsvps ?? []).find` throws rather than returning nothing.
- 2026-09-16 (#36): MUTATION-TESTED, two mutations with deliberately different blast radii: dropping `assignedRsvpIds` from the `/api/v1` feeder (the #78 shape) reddened 8 checks, every one an equality against a NON-EMPTY list; making the guest match honour only the FIRST assignee reddened exactly 3 — the second attendee, the third, and the third's read of the seat — while the first stayed green, which is the tight signal an intersection rule needs. AND THE REVIEW FOUND A THIRD MUTATION I HAD NOT RUN: a structural walk asserting a field's NAME is produced catches the line being DELETED and not the line being NEUTERED — `assignedRsvpIds: [] as string[]` kept all 76 boundary tests green while answering the exact wrong thing. Deletion is the easy mutation; neutering is the one that ships, and a cast is how it gets past `nuxt typecheck`. Refuse a bare literal (stripping `as T`) as well as an absent name, and do it at every call site rather than only the one the issue was about.
- 2026-09-16 (#37, corrected by its review): COUNTING THE USES OF AN IDENTIFIER PINS ONE SHAPE OF A REGRESSION, NOT THE REGRESSION. "`myRsvpIds` is read exactly twice in `listMediaForViewer`" catches the filter that re-reads the viewer's ids and MISSES `.filter(v => v.mine)` on the end of the chain — no new occurrence, the old behaviour restored exactly, 446 tests green. Assert over the EXPRESSION that must not narrow (no `.filter(` between `tickets: ticketRows` and the end of the return), scoped so the sibling buckets may still filter by type. And never let a PR body claim a cheap test covers something until you have run the CHEAP mutation, not just the expensive one.
- 2026-09-16 (#38): A COLUMN CAN BE CORRECT EVERYWHERE AND STILL BE A BUG, and the pipeline will be green for the whole life of it. `events_media.timeline_item_id` was written by nothing, read by `/api/v1` and dropped from `MediaItemView` — so the database, the contract test and the boundary walk all agreed while no screen could ever show it. Before believing a field is "already carried", `grep -c <field> server/domain/<the view>.ts`: the `/api/v1` feeder (`events-data.ts`) and the human view (`media.ts`) share no code, and an issue that says "check before adding a route" usually means the READ, which may be sound on one surface and absent on the other.
- 2026-09-16 (#38): A STRUCTURAL TEST THAT READS ONLY `<template>` LEAVES EVERY EXPRESSION IN `<script setup>` PINNED BY NOTHING, and that is where the mutations that ship actually live. Measured on this branch: `pinnedMediaByTimelineItem([])` in the card, `timelinePinnedMedia({tickets: [], documents: []})` on the page, and `.filter(m => m.mine)` inside the card's own getter each left eslint, `nuxt typecheck` and the smoke suite completely green. Pin the call site VERBATIM in the comment-stripped script (`ticket-scope.test.ts`'s `script()` helper is the precedent) as well as the `v-for`/binding in the template — one assertion per screen is not enough when the page, the card and the getter are three separate places the same value can be replaced. AND A NEW DOMAIN WRITE NEEDS ITS `elsewhere` RULE, not just a role-set assertion: adding `setMediaTimelineItem` to the role loop left a VERBATIM copy of its route on `/api/me` passing all 475 tests (#38 review). The `setTicketDetail` rule beside it is the shape — filter `guestHandlers`/`accountHandlers`/`machineHandlers`/`adminHandlers` for the DOMAIN FUNCTION, require that list empty, then assert the host surface really does call it so the rule is not about a function nothing uses. Also: a source grep over a guard (`isPinnableToTimeline(item.type)`) survives `if (false && …)`, so never let its `it(...)` sentence claim the guard REFUSES anything — say it is written, and name the smoke check that executes the status code.
## Moving a route between credentials

- 2026-09-14: MOVING A ROUTE OFF THE INVITE TOKEN SILENTLY DROPS EVERY CHECK `resolveInviteToken` DID — revocation, expiry, the use-count limit AND the `draft`/`cancelled` lifecycle refusal (`server/domain/invite.ts`). #48 moved expense writes onto a session and lost the lifecycle rule: an expense recorded happily against a cancelled trip, and no test noticed. Before moving a handler off a credential, read what the old resolver enforced BESIDES identity, and decide each one explicitly. `assertEventOpenToGuests` in `server/domain/permissions.ts` is now that rule, shared by both paths. It also drops revocation as a control surface for good: standing becomes "an RSVP row exists with my email", and an RSVP row OUTLIVES the link that produced it, so revoking an invite no longer withdraws anything the account can do — a consequence to name for the owner, not to paper over.
- 2026-09-14: When two gates answer the same verb (`addExpenseAsPlanner` on `/api/host`, `addExpenseAsParticipant` on `/api/me`), make their role sets AGREE and pin it with a test that reads both. #48 shipped a review round where `logistics` was 403'd on one surface and 200'd on the other. Take the narrower side: widening a role later is additive, un-widening after it ships is not.
- 2026-09-14: Gating a WRITE behind an account forces a decision about the READ that nobody asks for: the screen must tell an unidentified visitor why they cannot write, and the cheap way to do that — one condition for "show the card" — silently starts rendering the DATA to them too. Keep "who may write" and "who may see the figures" as separate props, and make the second one required so the next caller has to decide. A capability that did not move can still change what the page says by default.
- 2026-09-14: A gate that returns "the strongest standing" must not let a weak row short-circuit a stronger one. `assertParticipant` returned `logistics` on the planner row and never looked for the RSVP, so a logistics planner genuinely on the trip was refused by their own planner row.
- 2026-09-14: When a SECOND piece of code answers a question a gate already answers, it must draw the SAME line or the two contradict each other about one request. `findPlannerRoleBySlug` counted any planner row as planner standing, including `logistics` — the one role `assertParticipant` deliberately refuses to count — so the audit called a caller a planner for the very request the handler answered as a participant (or refused outright). Re-read the gate's exclusions, not just its happy path, before writing a cheaper version of it.
- 2026-09-14: A label branch for a case that cannot happen yet is still a decision. `/api/me` with no event in the path has no mutating route today; filing it as `participant` would have been a claim about an event not in the request the moment somebody adds `PATCH /api/me/profile`. It got its own kind (`account`) rather than a comment.

## Boundaries the tests hold, and what they actually assert

- 2026-09-16 (#29 review): A TRANSACTION IS NOT A LOCK, and "read-modify-write in one transaction" is the sentence that hides it. Under Postgres's default READ COMMITTED, `update … where expense_id = X` takes NO lock when it matches no rows — and it matches none in the rival transaction's snapshot precisely when the rival is about to claim the same slot, so two pins to one expense both landed. Lock a row that EXISTS IN BOTH SNAPSHOTS instead (`select … for update` on the parent, inside the transaction); a unique index also works and costs a two-statement dance around a constraint on every replace. Reproduce it with two `pg` clients before believing either the bug or the fix — and note `.returning()` on that update can come back EMPTY under a concurrent delete, so `rows[0]!` is a 500 waiting to happen. Nothing in `pnpm test` can execute an interleaving (no database); pin the MECHANISM structurally, run the interleaving by hand, and say which is which.
- 2026-09-16 (#29): THE GUEST PAGE'S BUDGET COMES OUT OF `GET /api/invites/{token}`, not out of `budget.get.ts` — `getInvitePage` builds it and SSR renders `/i/[token]` off that one resolve. Anything added to a budget has to be handled on BOTH, and that route names no budget in its own source. There are thirteen handlers that answer with a budget on the human surfaces; `removeExpenseAsPlanner` is not one of them (it answers `{removed:true}`).
- 2026-09-16 (#29): `/i/[token]`'s `complete` and `account` are resolved in the BROWSER (a stored identity and `useSession()`), so an SSR fetch of that page renders no expense list at all whatever cookie you send. Verifying a card change over SSR is therefore not possible — check the `__NUXT__` payload for the data instead, and grep `.output/public/_nuxt/*.js` for the new markup to confirm the template compiled. Nothing in this repository drives a browser.
- 2026-09-16 (#59): CURRENCY IS THE EVENT'S NOW (`events_event.currency`), and `events_instance_setting.base_currency` is only what a NEW event is created with — read once, at creation, by all three insert sites. The 409 in `setInstanceBaseCurrency` and `baseCurrencyHold` are gone. The columns on `events_expense` kept the names `base_currency`/`amount_base_cents` deliberately: Enterprise generates a client from them, and renaming them would break another repo to say what the `currency` beside them already says.
- 2026-09-16 (#59): THE RECOMPUTE IS THE ONE PLACE THAT DOES NOT APPORTION. On a write the per-person figures are derived from a total, so converting each separately invents liability (#61); on a currency change they already exist as debts people may have settled against, so re-apportioning would move money between friends silently. Each is re-expressed on its own and the difference goes on `Rounding` — which is why that account finally holds something. `buildEntryLines` still credits the payer the SUM OF THE DEBITS, so members net to zero per entry whatever the residual is, which is what keeps the settlement plan closing exactly.
- 2026-09-16 (#59): a `manual` figure is NEVER re-derived, including into the currency its own receipt is in. A EUR 245.25 bill that cost CHF 234.00 is worth the CHF figure re-expressed, not the receipt, when the trip moves to EUR — snapping it back would hand the payer the bank's fee to swallow. The identity shortcut (rate 1, shares = the receipt) therefore applies only to `fetched` rows, and getting that wrong is invisible to the unit tests and visible in `manualRatesKept`.
- 2026-09-14: since #25 the instance base currency is an INVARIANT, not just a default: every expense freezes `base_currency`/`fx_rate`/`amount_base_cents`, balances are the plain sum of the base cents, and `PATCH /api/admin/settings` answers 409 while any expense disagrees with the requested base. Loosening that refusal silently re-labels history — it is the thing that lets `loadBudget` state one currency over a column of sums.
- 2026-09-14: `suggestSettlements` walks debtors in BALANCE order (the list is sorted by net descending), so the SMALLEST debt is matched first. Any test pinning the plan has to expect that order; it is not "largest first" despite the doc comment's wording.
- 2026-09-14: `test/api-boundary.test.ts` enforces the three credentials and the admin gate's import boundary in both directions. When it fails, the route is in the wrong place or reads the wrong credential — it is not the test being fussy. It checks *where* code lives and what it imports; it cannot check that the gate you called is the right one for the route.
- 2026-09-14: `test/api-contract.test.ts` asserts a bijection between the `/api/v1` route tree and `docs/zaeme-api.openapi.yaml`. A route without a spec entry fails it, and so does a spec entry without a route. The bijection is over paths and methods — it says nothing about field names, optionality or status codes, which is exactly where a break for Enterprise hides. So ADDING a field passes every check while changing what Enterprise's generated client sees — say in the PR what changed shape, even when CI is green. It reads **no comments** either: the spec and the `/api/v1` handlers carry long prose CI cannot falsify (two comments were wrong about timeline editing from `bee78a3` until #10), and Enterprise vendors that YAML, so a stale comment there misinforms another repository. A comment-only diff is safe, and is owed whenever the surface it describes moves.
- 2026-09-14: `apportionCents(parts, total, newTotal)` is not only the FX helper — it is the general largest-remainder distributor, so a percentage or weight split is `apportionCents(weights, sumOfWeights, amountCents)` and needs no second rounding path (#26). A part of 0 provably never collects a leftover cent: leftover = sum(remainders)/total and every remainder is < total, so there are strictly MORE non-zero remainders than there are cents to hand out.
- 2026-09-14: A decimal a user TYPES and a column stores (`numeric(12,4)` for a percentage or weight) must be compared as an INTEGER at that scale — `33.33` is `333300` — or "these add up to 100" is a float comparison and 33.33+33.33+33.34 is not 100. Same discipline as `convertCents`'s BigInt; the refusal message then quotes the real sum (`add up to 99%, not 100%`), which is the difference between a fixable typo and a shrug.
- 2026-09-14: A RULE THE FORM AND THE SERVER BOTH APPLY BELONGS IN `shared/utils/` — Nuxt auto-imports it into app AND server (the precedent is `shared/utils/passkey-context.ts`; the app gets it by auto-import, server code by relative path, a test by direct import). #26 review caught the scale arithmetic living in both `server/domain/expenses.ts` and `BudgetCard.vue`, agreeing exactly and executed on one side only — the drift symptom being a running total reading `100% of 100%` beside a server that refuses the save. Keep the RULE shared and the REFUSAL server-side: return `null` rather than throwing, so the shared half carries no h3 dependency. #27 added the second: `isPlainEvenSplit` decides whether a recorded `even` split can be re-split at a new total, the server refuses with it and the form pre-empts that refusal with it, and two copies would mean one side re-splitting what the other calls unrecoverable — which moves money. When a refusal is right but the person cannot act on it from the screen they are on (a mixed-`even` 422 saying "send the split" on a screen whose split rows are hidden), PRE-EMPT it in the form with that same shared predicate rather than shipping a toast to decode.
- 2026-09-14: `docs/zaeme-api.openapi.yaml` carries the string `updateTimelineItem` in a COMMENT reserving the operationId for whoever decides to give the XO that verb. A test asserting the spec "does not mention" a name fails on the reservation — assert on a declared `operationId: <name>` line instead.
- 2026-09-14: `server/utils/passkey-bootstrap.ts` is imported by `server/utils/auth.ts` and by no route handler; the boundary test asserts that. The two circular cases (an unclaimed instance, a locked-out owner with `ZAEME_OWNER_BOOTSTRAP_TOKEN`) are decided against the database and the environment, never against anything the browser asserts.
- 2026-09-14: the instance owner is the first `zaeme_user` by `created_at` (`server/utils/instance.ts`), so a database with ONE account cannot express the 403 half of the admin gate — every session in it is the owner's. Seed two, with explicitly different `created_at` values: rows inserted in one statement can share a timestamp to the microsecond and make the owner whichever one the planner returns that day. Seeding one by hand: give the owner an explicitly OLDER timestamp than the other account, or the gate picks whichever the planner returns that day.
- 2026-09-14: driving the magic-link sign-in from a script needs an `Origin` header on `POST /api/auth/sign-in/magic-link` — better-auth's CSRF middleware answers `403 MISSING_OR_NULL_ORIGIN` without one, and `fetch` does not send it. The token is `zaeme_verification.identifier` (stored plain) and its `value` carries the email; match the row on THAT rather than on "the newest row", which picks the wrong token exactly when you are signing in two accounts in a row. The mail dry run's `[email:dry-run]` log prints the link twice (the `preview` field truncates the token, the `links` array does not) and is still the worse source: read the table.
- 2026-09-14: `/api/me/**` is the ACCOUNT surface — a session, no planner row required — and since #48 it holds expense writes as well as the `/me` aggregation. The gate is `assertParticipant` in `server/domain/permissions.ts` (planner row, or an RSVP whose `guestEmail` is the account's lowercased email). Guest-surface writes that need an identity belong here, never in `server/api/invites/**`.
- 2026-09-14: `server/middleware/audit.ts` records mutations at the edge; a new route needs no audit code. It must keep returning early for `/api/v1` and `/api/auth` — a change that drops those early returns puts a cookie read next to the machine surface.
- 2026-09-14: Adding a surface to `/api/v1` is a DECISION, not a default (#8). Enterprise generates MCP tools from the spec, so a route added there becomes a verb the model can call. Host and guest surfaces are the normal home for a new feature; `/api/v1` needs the owner's yes.
- 2026-09-14: Two sequential writes that SWAP a value are a trap: lose the second and the pair is left sharing it, after which every retry computes `a === b`, writes the same value twice, answers 200 twice and changes nothing — a dead control with no error. `sortOrder` reordering had exactly this shape in both timeline editors. Make a swap one statement, and have it renumber unconditionally so it also clears ties somebody else made.
- 2026-09-14: Two adjacent `string` parameters are a regression waiting to happen — transposing `(eventId, itemId)` type-checks, passes every structural test, and 404s the whole route in production. A named parameter object is the only thing in this repo that catches it; a test pinned to the call site cannot.
- 2026-09-14: A surface with no add and no delete has no escape hatch. Before shipping a read-plus-edit screen, ask what the user does when an edit wedges something — on `/admin` the answer cannot be "go to `/host`", because `/host` 403s the non-planning owner that surface exists for.
- 2026-09-14: A test that pins a SQL statement's SHAPE (a `row_number()`, one `update`) proves nothing about what it computes. Deleting a transposition, pinning a delta, and dropping a `where event_id` so a move renumbers every row on the instance all left `pnpm test` green. Since #49 there is a CI job with a migrated Postgres and an owner cookie — put the assertion in `scripts/api-smoke.sh`, execute the operation, and assert the RESULT. Raise `MIN_SMOKE_CHECKS` by exactly the number of checks added.
- 2026-09-14: When code recomputes positions from an ordering, every query the UI draws that list from must break ties on the SAME last key. The timeline renumbered by `…, created_at, id` while three list queries stopped at `created_at`; ids are cuid2, so on a tied list the first click moved a different row than the one the user pointed at. Assert it as one rule over every `orderBy` of the table.
- 2026-09-14: `is distinct from` in an UPDATE is not free: skipping unchanged rows also drops them from the statement's LOCK set, so a concurrent writer's change to them is never rechecked. If the operation claims an invariant over ALL the rows, open it with `select id … where … order by id for update` in a transaction — which also pins lock order and stops two writers deadlocking.
- 2026-09-14: `summariseAudit` groups on the COLUMN (`.groupBy(auditLog.actorKind)`), so a new `actorKind` shows up in the counts for free. The two places that do NOT learn it for free are the zod enum in `server/api/admin/audit/index.get.ts` and `ACTOR_ITEMS` in `app/pages/admin/audit.vue` — miss either and the owner silently cannot filter for that category (#51). `test/api-boundary.test.ts` now reads the enum out of the schema and asserts both agree.
- 2026-09-14: A boundary assertion of the form "X appears after the /api/v1 guard" is an assertion about the FILE, not about the code path: extract X into a helper declared above the handler and it breaks while the behaviour is unchanged. Pin the CALL SITE string (`resolveSessionActorKind(match.surface`), not the symbol. Same trap when two verbs SHARE a gate: "each function calls assertPlanner with these roles" then passes on the helper and says nothing about the callers — assert the roles on the helper AND assert each verb CALLS it, by call site.
- 2026-09-14: `/admin/audit`'s filters compose (`actorKind` + `surface` + `eventSlug`), and asking for `actorKind=planner&eventSlug=<slug>` alone also returns the `/api/host` rows for that event. Any assertion about what the ACCOUNT surface recorded must pin `surface=me` too, or it passes on a row from the surface it is not testing.
- 2026-09-14: since #61 the budget is a DOUBLE-ENTRY LEDGER: `events_account` (member/category/rounding per event), `events_expense` is a journal header and `events_expense_share` holds ALL its lines — signed, `account_id` never null, summing to zero in both currencies. `assertEntryBalances` refuses an entry that does not, as a 500, because every caller-fixable input was already refused upstream. The trip total is the sum of DEBITS into category accounts, never an account's net (which is zero by design), so a member-to-member transfer is excluded structurally and #28 needs no `kind` flag.
- 2026-09-14: base cents on a share are STILL apportioned as a group (#25), and #61 kept it after trying not to. Converting each share on its own to give the `Rounding` account something to hold does not surface a rounding artefact — it INVENTS a cent of liability and files it under a name that makes it look discovered: three people severally owe 83.68 for a thing that cost 83.67, the payer is credited 83.68 for handing over 83.67, `paidCents` stops meaning what they paid, and the error is bounded by N/2 cents PER ENTRY and accumulates. An account with nothing to do is the right answer, not a vacuous criterion; keep the mechanism (compute the residual, post a line when it is non-zero) and let #59 be what fills it. Generally: "this criterion would be satisfied vacuously" is not a licence to manufacture the thing it describes — ask first whether the vacuum is the CORRECT state, and if it is, ASSERT THE VACUUM and cover the mechanism with a direct unit call.
- 2026-09-14: A lazy seeder that runs on every read cannot be paired with a delete: `ensureEventAccounts` re-created the `Tickets` account the moment anybody looked at the budget after removing it, and only the smoke check that asserted it was GONE noticed. The rule that works: seed the full starting set only when the event has no accounts at all, and afterwards ensure only what the code genuinely depends on (`Uncategorised` and `Rounding`, both `isSystem`).
- 2026-09-16: `test/api-contract.test.ts` freezes EXPOSED_TOOLS *and its length*, so adding a `/api/v1` operation fails it until the operationId is added BY HAND. That is the deliberate gate on Enterprise's tool vocabulary (33 -> 34 with `updateTripExpense`), not a test to route around — and the diff to that list is the honest place to say the model's vocabulary grew.
- 2026-09-16 (#31): `Intl` IS FOUR SEPARATE SURPRISES. (a) IT ACCEPTS MORE THAN AN IANA ZONE — `+02:00`, `Etc/GMT+5`, `GMT` and `UTC` all resolve, and each is a fixed offset that reads correctly in March and an hour wrong in April. EXCLUDING THE BAD FAMILIES BY NAME NEVER ENDS: the first rule excluded `Etc/` and still took `SystemV/EST5EDT`, which follows the PRE-2007 US daylight rules and is an hour off `America/New_York` for three weeks every March — wrong just rarely enough not to be reported. The answer is an ALLOW-LIST of IANA's ten geographic areas (Africa America Antarctica Arctic Asia Atlantic Australia Europe Indian Pacific), which is safe to be that strict about because `Intl.supportedValuesOf('timeZone')` contains exactly those ten and nothing else — so it can refuse nothing any picker offers. (d) `dateStyle`/`timeStyle` AND `timeZoneName` CANNOT BE COMBINED: `Intl` THROWS `TypeError: Invalid option : option` rather than ignoring one, so stamping a zone onto a `dateStyle` string needs a second formatter and a join. (b) `resolvedOptions().timeZone` canonicalises `US/Eastern` to `America/New_York` but also `Europe/Kyiv` to `Europe/Kiev` and `Asia/Kolkata` to `Asia/Calcutta` on Node 22, and `Intl.supportedValuesOf('timeZone')` lists the OLD names — storing the resolved value puts a name on the host's screen they did not type, so #31 keeps it only when it differs by CASE alone. (c) `Intl.Locale.prototype.timeZones` (a getter in V8, `getTimeZones()` in JavaScriptCore) answers a REGION's zones, which is how a geocoded place suggests one with no country-to-zone table in the repo — Nominatim needs `addressdetails=1` for `address.country_code`, and the COUNTRY CODE is what belongs in the thirty-day cache while the zones are derived on the way out.
- 2026-09-16 (#31): A DISPLAY ZONE IS A LABEL AND MUST MOVE NOTHING STORED — and no unit test can see the difference: a mutation that rebased `starts_at` onto the new zone in `updateEvent` left all 393 vitest tests green and reddened 4 smoke checks. Write the fixture instant BEFORE the label changes, read it back AFTER on more than one surface, and compare against the value written rather than a constant. The converse obligation is bigger than it looks: EVERY `datetime-local` that writes a time on a zoned event has to be READ against that zone too, or saving an untouched form moves the item — `HostTimelineCard`, `HostPlacesCard`, `GuestLegCard`, `HostSeriesCard`, `host/new.vue`, the date-poll option on `host/[slug].vue` and `AdminEventTimeline`, the last of which edits the same column `/host` does. AND IF YOU KEEP THE SPELLING THE USER TYPED, EVERY COMPARISON HAS TO KNOW IT: one `zones.includes(stored)` undid that decision — a trip on `Europe/Kyiv` is not in ICU's `['Europe/Kiev', …]`, so the card re-offered the trip its own zone under the alias the decision existed to avoid. A `sameZone` that resolves both sides is the only honest comparison. That conversion needs TWO PASSES (the offset must be looked up at the instant being computed; one pass is right all year and wrong near a transition), and its two edges need DEFINED answers pinned by a test — #31 resolves the hour that does not exist FORWARDS and the hour that happens twice to the SECOND occurrence, which makes a `datetime-local` round trip lossy for exactly one hour a year.
- 2026-09-16: on a PATCH, ABSENT is not `null` and neither of them is "the default". `resolveCategoryAccount(accounts, {})` answers `Uncategorised`, so calling it unconditionally on an edit moves every edited expense out of its category — and stays green under vitest, lint, typecheck and every other smoke check. Branch on `input.x !== undefined`, and assert the LEAVE-IT-ALONE case explicitly; nothing else in the pipeline can see it.
- 2026-09-16: `signMediaItems` spreads the whole `MediaItemView`, so a field added to that view reaches every signed surface for free — which is right, and is also why a field that must NOT reach guests cannot be added there. `/api/v1` is unaffected either way: it projects through `v1-shapes.mediaItem` and carries no URL.
- 2026-09-16: a nested selection object over a drizzle `leftJoin` makes "no joined row" and "a joined row of nulls" indistinguishable. Select the child's columns FLAT and rebuild the object off the child's primary key as the sentinel, whenever the empty child row is a state the product cares about.
- 2026-09-16: `v1-shapes` normalising a missing field (`Array.isArray(x) ? x : []`) means the #78 class arrives on `/api/v1` as a PLAUSIBLE EMPTY and never as an absent key — so a smoke sentinel for "the field is gone" can never fire there. It fires only on the host and invite surfaces, which answer the domain view with no shape layer between. The catch on `/api/v1` is asserting the EXPECTED ids, never "not empty".
- 2026-09-16: a walk asserting "this name appears NOWHERE" fails on PROSE — this repo's comments explain what a change replaced, so 8 files legitimately carried `assignedRsvpId` in a sentence (the same trap `updateTimelineItem` set in the spec). Pin the CODE forms instead (`\.name\b`, `\bname\s*[:?]`, `['\"(]snake_case['\")]`), exempt the file stating the rule and any file whose job is to speak the PREVIOUS release's contract, and count what the walk found before believing an empty offender list.
- 2026-09-16 (#38): `media_field` PRINTS `null` FOR A MISSING KEY AND FOR A NULL VALUE, so any check built on it cannot tell "nothing is pinned here" from "this surface stopped carrying the pin" — which is the whole defect wearing a passing tick. Any new nullable field needs its own three-way helper (`no-such-item` / `no-field` / the value), the shape `ticket_field` and `guest_field` already have; and a per-step SET helper beside it, because a `contains` on one filename cannot tell a pin from a smear and cannot see a MOVE at all (the pin leaving one step as it arrives at the other is two set assertions).
## Running things locally (smoke scripts, a scratch database)

- 2026-09-16 (#29, merging #59 and 2026-09-14): `MIN_SMOKE_CHECKS` IS NOT EQUAL TO THE GREP COUNT — on `750c083` the floor is 521 while `grep -cE '^[[:space:]]*(check|contains|excludes|equals|ledger_imbalance|plan_closes|entry_lines|entry_order|entry_shares|entry_weights|leg_route|leg_orders|geocode_shape) '` says 519. Only the DELTA is meaningful: run that expression on `main` and on your branch, add the difference, and confirm against a real run (57 added, 521 -> 578, `passed 578`). The LEADING-WHITESPACE form matters — every check inside an `if [ -n "$ZAEME_TEST_*_COOKIE" ]` block is indented and counts too — and a check you only EDITED nets to zero, which is right. `scripts/api-smoke.sh` SKIPS rather than fails and still exits 0, so a bare local run is 62 short of the floor (the poster block is 12 and the receipt block's byte half 48, each skipping as ONE line, and TWO more genuinely FAIL — `server/api/me/events/[slug]/media/presign.post.ts` calls `assertStorageConfigured()` BEFORE `requireGuestUser`, so an unauthenticated probe is 501 rather than 401 with no bucket) and "passed 518, failed 0" is the same shade of green as 578, as is `passed 565, failed 2` against a floor of 627: do the subtraction in the report, or better, run with storage. Run the suite TWICE while you are there — it re-runs against the previous run's rows and a non-idempotent check fails only the second time. The `api` job greps for `skip` lines for exactly this reason; keep that assertion when you touch it, and raise the floor rather than lowering it.
- 2026-09-16 (#59): `cd X && node server &` FOLLOWED BY MORE COMMANDS RUNS THE REST IN THE ORIGINAL CWD — `&` splits the list, so `cd /worktree && … & sleep 7; ./scripts/api-smoke.sh` runs MAIN's copy of the script against your build. The tell is failures naming checks your branch deleted. Start the server in its own call and invoke the script by ABSOLUTE path. Same precedence, worse symptom: `. env.sh && setsid nohup A & setsid nohup B & setsid nohup C &` puts only A inside the sourcing, so B and C boot with NO environment — one service up and the next two dead with "DATABASE_URL is not set" and an EADDRINUSE on a DEFAULT port the override never reached. One service per call, each sourcing the env itself.
- 2026-09-14 (#63): `pg_dump -Fc` the database at N-1 ONCE and `pg_restore` it into a fresh database per attempt — a mutation of a migration is then a 2-second loop instead of rebuilding and re-smoking the previous release (~4 minutes) every time. Two full builds plus two smoke runs of a base/head rehearsal cost about four minutes locally; `pnpm build` is ~85s. `scripts/api-smoke.sh` DELETES every expense it can reach through `/api/v1` in its base-currency block, which is near the TOP of the file. Anything that inspects rows written before the run has to happen first, or it is asserting against a budget the suite just emptied.
- 2026-09-14..16: A SERVER STARTED FROM A TOOL CALL IS A LIABILITY IN BOTH DIRECTIONS. A bare `cmd &` dies with the call's process group (every later curl answers `000`), while `setsid nohup <script> > log 2>&1 < /dev/null &` — or the Bash tool's own background mode, which is better because it can be stopped by id — survives; and a survivor from an EARLIER call keeps the port, so the next start exits 0 WITHOUT BINDING and curl answers happily from the stale build (the tell is one check changing answer for no reason). `pkill -f "PORT=<port>"` does NOT kill it — the pattern matches the wrapper shell, and `node .output/server/index.mjs`'s own argv carries no port — so identify it by `tr '\0' '\n' < /proc/<pid>/environ | grep PORT=` and `kill -9` that pid, in a SEPARATE tool call from the one that starts the next server (`ps aux | grep "[i]ndex.mjs" | xargs kill -9` inside a backgrounded call kills its own wrapper, whose argv contains the whole command line). And because `pnpm build` takes 3-5 minutes here, a five-mutation campaign is ~30 minutes of wall clock: SCRIPT it as one background run (back the files up, then apply -> build -> restart -> smoke -> vitest -> revert), and `diff -q` against the backups before committing. CONCURRENT AGENTS STEAL PORTS too: a sibling worktree's server on your port answers with its own token and database, and the run then reports ~100 failures that look exactly like your change broke everything — `ss -ltnp` and `/proc/<pid>/environ` before believing a red smoke run, and pick a port nobody else would guess.
- 2026-09-14: There is no docker daemon here, but Postgres 16 binaries are: `su postgres -c "/usr/lib/postgresql/16/bin/initdb -D <dir> -U zaeme --auth=trust"` then `pg_ctl -D <dir> -o '-p <port> -k <sockdir>' start`, which is enough for `scripts/migrate.mjs` and a full `pnpm smoke:api`. NOT into the agent scratchpad: `initdb` runs as `postgres` and `/tmp/claude-0/...` above it is `drwx------ root`, so it cannot traverse the path whatever you chmod the leaf — use `/var/lib/postgresql/<name>`, `chown postgres:postgres` it, put the socket dir there too, and pick a port nobody else would guess. The shared cluster is not always running; check before assuming the memory's "there is already a Postgres" still holds. When it IS running, the fastest private database is `su postgres -c "psql -c \"create role <name> login password '<pw>'\""` then `su postgres -c "createdb -O <name> <name>"` — root cannot authenticate as `postgres` over TCP or the socket, and this touches nothing the `zaeme` role anybody else is using. THE CLUSTER IS STILL SHARED, THOUGH: mid-run, every request started answering 500 with `permission denied for table zaeme_audit_log` because a sibling's cleanup had reassigned my database's tables to `zaeme`. `select tablename, tableowner from pg_tables` says so in one line; the fix is a fresh `createdb -T template0` under a name nobody would collide with, not a fight over grants. And the S3-gated checks can be run here too, without a container runtime: ~40 lines of node holding objects in a `Map` keyed by `<bucket>/<key>`, answering PUT/GET/HEAD/DELETE and echoing the stored `content-type`, satisfies every one of them. `adobe/s3mock` is signature-blind as well, so the stub loses nothing they depend on — what it CANNOT prove is that a presigned signature is valid, which is why the URL assertions are about its shape (`X-Amz-Signature=`, `X-Amz-Expires=`) and the object assertions are a sha256 of the bytes fetched back. Keep the stub in the scratchpad, not the repo. That combination is what makes a ZERO-SKIP `pnpm smoke:api` possible here at all; assert the bytes with a sha256 of what comes back rather than trusting the stub.
- 2026-09-14: `.claude/agent-memory/implementer/MEMORY.md` is the file that CONFLICTS on rebase — every concurrent implementer appends to it. Resolve by keeping BOTH sides (the lessons are additive), never by taking one. AND IT EXISTS IN THE MAIN CHECKOUT TOO, WHICH IS SOMEBODY ELSE'S BRANCH — editing `/home/user/zaeme/.claude/...` instead of `<worktree>/.claude/...` dirties another agent's working tree with your lesson. Write the worktree's copy.
- 2026-09-14..16 (merged): ANY SMOKE BLOCK THAT NEEDS A LIVE EVENT MINTS AND PUBLISHES ITS OWN. `STATUS_TRANSITIONS.completed` and `.cancelled` are `[]` (`server/domain/events-data.ts`) — both TERMINAL, so a line that "restores" a completed event to `published` 409s into `/dev/null` and reads as if it worked — and an invite token only resolves on a live one (`resolveInviteToken` 403s on `draft` and `cancelled`, `server/domain/invite.ts`), so a check against `/api/invites/**` that skipped the publish fails for a reason that has nothing to do with what it is testing.
- 2026-09-14: `scripts/api-smoke.sh` also proves the human half of the credential boundary, but only if you give it `ZAEME_TEST_SESSION_COOKIE` (the owner) and `ZAEME_TEST_GUEST_COOKIE` (anybody else). Without them it silently checks the machine surface alone. Get a cookie by POSTing to `/api/auth/sign-in/magic-link`, reading `identifier` from `zaeme_verification`, and following the verify URL with `curl -c` — or just `node scripts/ci-smoke-setup.mjs seed` then `sign-in`, which prints both export lines. That script seeds `owner@example.com` and `planner@example.com`, but a check needing an account's own address should ask better-auth (`GET /api/auth/get-session`, first `"email"` in the body) rather than hardcode one: a human runs the same script with their own two cookies.
- 2026-09-14: The audit table is `zaeme_audit_log` and its timestamp column is `at`, not `created_at`. It is where you confirm a new route is audited without writing audit code — `eventSlugFromPath` matches `/events/<slug>` anywhere in the path, so `/api/admin/events/<slug>/…` is pinned to its event for free. The audit row is written from the response's `finish` hook — AFTER the body is on the wire — so a smoke check that writes and then immediately reads `/api/admin/audit` can legitimately beat it. `audit_await` in `scripts/api-smoke.sh` polls for it; do not swap it for a bare read that passes on your machine.
- 2026-09-14, CORRECTED 2026-09-16 (#74 review): A PLANNER INVITE **IS** REDEEMABLE OVER HTTP — `POST /api/host/join/<token>/accept` with a session (`acceptPlannerInvite`), which the old form of this line said did not exist. That is how a smoke check gets an account with a SPECIFIC planner role: mint with `POST /api/v1/events/<slug>/planner-invites -d '{"role":"logistics"}'`, accept it with the second cookie, and the 403 you then assert is about the ROLE rather than about having no standing — assert the acceptance (`"role":"logistics"`) too, or an invite that never landed makes it the no-standing check wearing another name. The other way, still true, is to have the second account create its own event with `POST /api/host/events`, which makes it that event's owner planner.
- 2026-09-16 (#74 review): A GUARD THAT NAMES ONE FIELD LEAVES THE SHAPE OPEN. "A transfer may not become a cost" was written as one refusal (`accountId`/`category`) while `participants`, `splitMode` and `title` were still accepted on the same entry — so a PATCH could split a two-line payment three ways, and the derived title went on naming a pair that had been corrected. When the rule is "X may not become Y", enumerate every field that DEFINES Y's shape and refuse each, then assert the shape itself before writing (`resolved.length !== 1` as a 500, unreachable from a request by construction). A shape defended at one field is defended nowhere.
- 2026-09-16 (#28): THE HOST PAGE'S `BudgetCard` DOES NOT RENDER IN SSR HTML — it is behind `me.email`, which `useSession()` only resolves in the browser — so `curl /host/<slug>` finds none of its strings and proves nothing about a card change. What a terminal CAN prove: `pnpm build` compiles every Vue template (a syntax error fails it), `nuxt typecheck` resolves the auto-imports, and `grep -o 'Mark as paid' .output/public/_nuxt/*.js` shows the component AND its inlined `shared/utils` helper actually reached the client bundle.
- 2026-09-15: "NO CHECK DEPENDS ON THE THIRD PARTY ANSWERING" IS A DIFFERENT CLAIM FROM "NO REQUEST IS SENT". #32's smoke salts its queries per run so the cache cannot spare them — correct for testing a cache, and ~6 live requests per suite run, twice per CI run, on every push and PR, from shared runner addresses under the same User-Agent prefix production sends. A block earned by CI is served to the owner's instance. Give every outbound client a base-URL environment variable, point CI at a stub script, and CHECK the stub's own request log rather than believing the claim. The stub must be WAITED FOR, not merely started: a degrading client answers 200 while it is down, so every shape assertion passes on the outage path and the job is green about nothing — wait on its `/healthz`, and assert in vitest that the workflow still does, because the property is one deleted line away from ending silently. It IS reachable from the agent box and from a booted build there, which is exactly what makes the distinction easy to lose.
- 2026-09-16: the smoke suite needs the GEOCODER STUB too (`GEOCODER_STUB_PORT=<port> node scripts/geocoder-stub.mjs`, and `ZAEME_GEOCODER_URL` pointed at it). Pointing that variable at a dead URL fails the place-search block for a reason that has nothing to do with your diff; leaving it UNSET points the run at OpenStreetMap, which is somebody else's public service.
- 2026-09-16 (#38): THE AGENT BOX HAS A DOCKER CLIENT AND NO DAEMON, and `repo1.maven.org` 404s through the proxy, so neither `adobe/s3mock` route works — but the whole S3 surface this app uses is path-style PUT/GET/HEAD/DELETE at `{endpoint}/{bucket}/{key}` with the signature ignored (s3mock is signature-blind too). A ~50-line in-memory node stub in the scratchpad runs the FULL suite including the 40 bucket-gated pinned-media checks and the 12 poster ones; without it the local run is short by exactly those and proves nothing about the feature. Postgres here is the shared cluster with a `zaeme` role (`postgres://zaeme:zaeme@127.0.0.1:5432/<your db>`, created with `sudo -u postgres createdb -O zaeme zaeme<issue>`); `su postgres -c …` is refused by the worktree guard, `sudo -u postgres` is not. WATCHING CI FROM HERE HAS TWO TRAPS OF ITS OWN: `gh` IS NOT ON `PATH`, so a poll loop built on it fails silently and waits forever (use the GitHub MCP tools); and `pull_request_read`/`get_check_runs` served a job as `in_progress` for 25 MINUTES after it had finished — `actions_list` with `list_workflow_jobs` gives the truthful per-step view, and `get_job_logs` answering 404 is the honest "not finished yet". Budget for the `upgrade` job being genuinely slow too: two builds and two full suites, ~5 minutes after the other two are green.
## Deploys, builds and the platform

- 2026-09-14 (#63): BRANCH DELETION IS BLOCKED from this environment — `git push origin --delete` answers "the remote end hung up unexpectedly" and then "Everything up-to-date" (the branch survives), and the REST `DELETE /git/refs/heads/...` answers 403 "Write access to this GitHub API path is not permitted through this proxy". So a scratch branch pushed for evidence is permanent until a person deletes it: push one only when the run is worth it, name it so nobody merges it, and say in the PR that it is still there.
- 2026-09-14 (#63): with `push` + `pull_request` both on and one concurrency group, opening a pull request CANCELS the push run for the same commit (observed: run 34892127007 cancelled mid-`ci` the moment 34892164055 started). That is the dedupe working, not a failure — but the commit then carries a cancelled run beside the green one, so say so before somebody reports it as flaky CI.
- 2026-09-14 (#63): drizzle's migrator decides what to apply from the JOURNAL's timestamps, never from the file's content, and records applied migrations in `drizzle.__drizzle_migrations` (`id`, `hash`, `created_at`). So editing a migration that a deployed instance has already applied changes nothing anywhere — not in CI's `upgrade` job and not in production — and counting that table either side of `migrate()` is the only way to tell "applied yours" from "applied nothing", which is what `scripts/migrate.mjs` now prints. They are generated and are not mergeable text either: after a rebase that brought in another migration, REGENERATE and re-verify rather than resolving by hand.
- 2026-09-14: Merging to `main` ships. Kitchen builds the commit with buildpacks per `kitchen.json` and the `migrate` task runs on the way in; **a failed task stops the deploy**, leaving production on the previous release. The symptom is "my change is not there", not an error page.
- 2026-09-14: `engines.node` in `package.json` is read into `BP_NODE_VERSION` by Kitchen and handed to the node-engine buildpack verbatim. Dropping it as boilerplate silently upgrades Node on the next build.
- 2026-09-14: `kitchen.json` is the only honest answer to "what does a deploy run": `node scripts/migrate.mjs`, plain .mjs because a production install prunes drizzle-kit away. `pnpm db:migrate` (`drizzle-kit migrate`) is LOCAL only and runs under a different dependency set, so reproducing a failed deploy with it can succeed where the deploy failed. `server/database/drizzle.config.ts`'s header claimed the opposite until #54/#55 corrected it.
- 2026-09-14: Grepping for a wrong claim finds mostly RIGHT ones: 5 mentions of `db:migrate` outside the memory, and only the config header was wrong (#54). `README.md:94` uses it legitimately for a from-a-clone self-host, where the dev install has drizzle-kit — so "local development against a scratch database" would have been too narrow a correction to write. Read each hit's surrounding paragraph before calling it stale; a command can be right in one context and wrong in the next.
- 2026-09-14: There is no Dockerfile and no workspace — zäme is a single flat package. A change that assumes `packages/*` or a build stage is thinking of the pre-transplant layout or of Enterprise.
- 2026-09-14: `actions/setup-node` with `node-version-file: package.json` reads `engines.node` — the same field Kitchen turns into `BP_NODE_VERSION`. Use it in any new job rather than hard-coding a major, so CI and the buildpack cannot drift apart silently.
- 2026-09-14: writing a secret to `$GITHUB_ENV` does NOT keep it out of the build log — the runner reprints a step's whole `env` block in the next step's group header. It needs `::add-mask::` on stdout BEFORE the value is first printed, which means a script whose stdout is redirected into `$GITHUB_ENV` can never mask anything: have it append to the `$GITHUB_ENV` file itself and keep stdout for the workflow command. Escape `%` as `%25` in the mask payload first, or a percent-encoded value (every better-auth cookie) masks a string that never appears.
- 2026-09-14: MinIO is gone as a CI dependency — the open-source server was archived, `dl.min.io` answers `410 Gone` and the `minio/minio` Docker Hub repo is unauthorized. The `api` job uses `adobe/s3mock` instead: path-style, in-memory, signature-blind, and it takes its bucket from `COM_ADOBE_TESTING_S3MOCK_STORE_INITIAL_BUCKETS` with no command arguments, which is what lets it be a plain `services:` entry.
- 2026-09-15: POSTing to a path that has only a `.get.ts` handler answers **404**, not 405 — which is what "a guest cannot mint a place over the link" is asserted with (`server/api/invites/[token]/places.get.ts`). Same shape the #48 checks use for the expense routes that no longer exist.
- 2026-09-15: AN ORDER THAT TWO SURFACES COMPUTE HAS TO BE ASSERTED ON BOTH, or the assertions agree with each other and not with the product (#30 review). Every reorder check read `/api/host/.../places`; the guest page renders through `mergeItinerary`, which keyed on the clock while the query keyed on `sort_order`, so a planner could move a timed leg, watch it move on /host and find the invite link unchanged — sixteen green checks, none able to see it. When a rule is written down twice, one of the two checks must read the OTHER one's output.
- 2026-09-15: A FORWARDABLE CAPABILITY URL'S PAYLOAD IS A PRIVACY SURFACE, and `contains` cannot state what must not be in it. Answering a row verbatim to `/api/invites/**` put every place's `note` ("lockbox 4417, back door") and address in the SSR'd page source of `/i/<token>` — the `addedByEmail` shape from #61 with discoverability added. Narrow at the domain boundary (`guestPlaceView`), route BOTH guest reads through it, and assert it with the new `excludes` helper in `scripts/api-smoke.sh`.
- 2026-09-15: a CHECK constraint is worth the line when a rule spans two writers: "both or neither" on `(lat, lng)` held only in `normaliseCoordinates`, and a half-coordinate row inserted by hand is UNREPAIRABLE from the product — the edit form sends the pair back, the domain 422s every save of that place including a rename, and the screen says "no coordinates yet" and explains nothing. Reviewers do insert such a row.
## Types that stop guarding

- 2026-09-16 (#59 review): `currency !== baseCurrency` IS THE WRONG QUESTION for "was this converted", and it reads like the definition. A payer's stated out-of-pocket figure survives a change of the trip's currency, so a EUR receipt on a trip that moved to EUR has both codes equal and the two amounts nine francs apart — the card printed the receipt, hid the settled figure and hid the rate line that would have explained it, and the budget called itself exact. The rate is the only field that survives every path: `shared/utils/conversion.ts`, one rule, read by the server and the card.
- 2026-09-16 (#59 review): A DERIVED COLUMN THAT OVERWRITES ITS OWN INPUT DESTROYS SOMETHING UNRECONSTRUCTABLE. The recompute rewrote `amount_base_cents` on a row whose payer had typed that figure off a bank statement and kept no copy, while `fx_rate_source` went on saying `manual`. Keep the stated input beside the derived one (`stated_amount_cents`/`stated_currency`) in the SAME migration — a column you wish you had kept cannot be added retrospectively once real money is in the table, and preserving it leaves the behavioural question open for the owner instead of guessing.
- 2026-09-14: `db.execute<T>(sql`…`)` returns a pg `QueryResult`, not an array — `[...result]` does not type-check and `result.rows` is the list. `db.select()` returns the array directly. Two shapes in one module.
- 2026-09-14: typing a helper that runs inside a drizzle transaction as `Db | <transaction type>` does not compile; the transaction generic is internal and changes shape between minor versions. `Pick<ReturnType<typeof useDb>, 'select'>` names the one method the helper needs and survives the upgrade.
- 2026-09-14: a drizzle `numeric(p, s)` column comes back from Postgres PADDED TO ITS SCALE — `'1'` written to `numeric(20,10)` reads back `'1.0000000000'` — so every string assertion and every rendered rate needs the trailing zeros trimmed on read. Nothing type-checks this: the column is `string` either way. READ BACK THROUGH `Number()` is how a coordinate becomes usable arithmetic — the column stays exact decimal (what was typed is stored, and it sorts and indexes for a bounding box) while the API view is a JS number, because every consumer of a coordinate does arithmetic and a driver string is a `+` away from concatenation; money is the opposite case and stays integer cents. And `Number(n.toFixed(6))` decides an EXACT half (…5 in the seventh decimal) from the binary representation, so `8.5416575` rounds DOWN — do not pin a half-case in a test, pick a fixture off the boundary and say why, and let a shape check allow the CALLER's precision rather than the column's (`String(47.37688659999999)` is what a geocoder really sends).
- 2026-09-14: A guard typed `(ev: { status: string })` accepts any row with a `status` — an RSVP, an invite — and keeps compiling if a value in `text('status', { enum: [...] })` is renamed out from under it, leaving the comparison matching nothing with `pnpm typecheck` green. Type a schema-value guard `Pick<typeof tables.X.$inferSelect, 'field'>`. Same shape as `events_expense.currency`: a value nothing notices has stopped matching.
- 2026-09-14: reading two tables into a lookup map in the WRONG ORDER is a live concurrency bug, not a style point. `loadBudget` read accounts, then lines; a concurrent write that created a new member account in between left a line whose account was not in the map, and the `?? 'category'` fallback then added somebody's share to the trip total while its owner vanished from the balances. Read the lookup side SECOND so the map is a superset, and prefer a fallback that is obviously wrong on screen to one that is silently plausible.
- 2026-09-14: rows written by ONE insert share `created_at` to the microsecond — Postgres `now()` is transaction time — and cuid2 ids do not sort by age, so `order by created_at, id` over them is stable but ARBITRARY. If the order is part of the contract (the lines of an entry, the shares in the order a person named them) it needs an explicit `seq` column; a comment claiming the written order while the query cannot produce it is two committed comments disagreeing.
- 2026-09-14: a plain `account_id` reference lets a child row point at a parent belonging to a DIFFERENT event. The fix is a composite foreign key `(event_id, account_id) → events_account(event_id, id)` plus the unique index on `(event_id, id)` it needs as a target — Postgres then refuses it instead of the code remembering to.
- 2026-09-15: `$fetch(url, options)` with a plain `string` url makes Nuxt's typed-route inference recurse until the compiler gives up — `TS2321: Excessive stack depth`, six copies of it, only under `nuxt typecheck`. eslint and vitest are both silent. A template literal resolves (that is why `server/utils/fx.ts` compiles); a variable does not. Declare the narrow call shape you actually use and cast `$fetch` to it once.
- 2026-09-15: A FIELD NO SCREEN AND NO LOG READS IS A COMMENT PRETENDING TO BE AN ENUM. #32's three `unavailable` reasons reached neither: the card branches on `status` alone, correctly (there is nothing a planner can do about any of them), so the distinction existed only in the type. One `console.warn` on the failure path is what makes it real — it is the only place "OpenStreetMap is busy" and "this instance is blocked" differ, and the second needs a person. Its twin: a WRITE-ONLY field in a form is a rule with no way out — the card set `osmType`/`osmId` on create and offered nothing to clear them, so a place pinned to the wrong feature could never be re-pinned.
- 2026-09-16: a Vue `watch` that RESETS state on change will eat a prefill written in the same tick — `splitMode` clearing `splitValues`, `spentCurrency` re-fetching a rate over the frozen one. A one-shot suppression flag the watcher consumes is deterministic where relying on `nextTick` ordering is not.
- 2026-09-16 (#38): WHEN A `.vue` FUNCTION STARTS BEING SAID IN TWO SCREENS, MOVE IT TO `shared/utils/` RATHER THAN COPYING IT. `assignedLine` was four lines in `MediaGallery.vue` and correct; a second copy on the itinerary would have been two screens able to disagree about one row, with no check on either because nothing here executes a `.vue` file. The move costs one line in the existing structural test and buys the rule a vitest that runs it. THE SAME QUESTION ABOUT A READ HAS THE OPPOSITE ANSWER AND I GOT IT WRONG ONCE: two cards on one page that render the same client-fetched list must SHARE it under an EXPLICIT `useFetch` key (`key: \`host-media-${props.slug}\``, written out in both files), not own a `$fetch` each. I gave each its own read to avoid an auto-generated key's invisible cache coupling — a good instinct aimed at the wrong thing: the objection is to the key being IMPLICIT, not to sharing. The cost was a shipped bug (#38 review): upload a ticket in `HostMediaCard`, and it was absent from `HostTimelineCard`'s pin picker until a page reload, because that card's `refresh()` moved only its own copy — the exact round trip the issue existed to delete. Nothing caught it: lint, typecheck, 475 tests and 879 smoke checks all green, because the smoke suite is curl and never loads a Vue page. Pin the key LITERAL in both files with a test; a drift of one character silently restores two entries.
- 2026-09-16 (#38): A NEW NULLABLE WRITE IS ONE PUT, NOT A POST PLUS A DELETE — `{"timelineItemId": null}` is the un-pin, so no second route can disagree about what re-pinning means. Make the field REQUIRED and nullable in the zod schema: optional would make a forgetful client's request a silent un-pin, and un-pinning is the one outcome a person cannot tell from "it did not save". And a pin over a nullable column on the CHILD needs no `FOR UPDATE` the way `pinReceipt` does — that lock exists for "at most one receipt per expense", an invariant over other rows; "at most one step per file" is the column itself.
## Style and shape

- 2026-09-16 (#29 review): "THIS WIDENS NO READ SET" IS AN ARGUMENT ABOUT THE SERVER AND NOT AN ANSWER ABOUT THE SCREEN. Pinning a receipt let nobody see a byte they could not already fetch from the gallery — correct, reviewed, and still wrong on the surface, because the control was a glyph and the word "gallery" appeared exactly once in the flow: in the toast you get when you take a receipt OFF. Somebody photographing a hotel folio is thinking "evidence for my friends", not "publication". Say where the thing goes BESIDE THE CONTROL, before the decision, not in the confirmation after it — the #67 shape, where a field labelled "Note (optional)" said nothing about who could read it. Withholding the data instead would have been theatre; the fix is a sentence.
- 2026-09-14: Stock Nuxt UI with its default look. The Enterprise design system, the `ui-components` and `navigation` skills, and every instrument-panel idiom are for the other repo and are a review finding here.
- 2026-09-14: `AGENT.md` is the style contract for server code: `#server/...` aliases over deep relative imports, `e.method` over `getMethod(e)`, return the query directly, modern Zod (`z.email`, `z.url`, `z.iso.datetime`).
- 2026-09-14: Domain logic goes in `server/domain/`; handlers stay thin. Cross-event admin reads go in `server/domain/admin.ts` — read the N+1 note at the top of that file before adding one.
- 2026-09-14: Ask `server/utils/mail-status.ts` whether this instance can send mail. Reading `RESEND_API_KEY` directly misses the mail relay in front of Proton Bridge (`KITCHEN_SERVICE_MAIL`) and the dry run.
- 2026-09-14: order two refusals by what the person can ACT on. `removeAccount` checked "holds lines" (409, delete your expenses) before "is a system account" (422, never), so the first answer sent somebody to empty `Uncategorised` and the second refused them anyway. The unconditional refusal goes first.
- 2026-09-15: DRIZZLE-KIT EMITS EVERY CONSTRAINT BEFORE EVERY INDEX, so a composite foreign key whose target is a `uniqueIndex` created in the SAME migration aborts with `42830 there is no unique constraint matching given keys` — on an empty database as well as a full one, which as the Kitchen `migrate` task is a deploy stranded on the previous release. Move the `CREATE UNIQUE INDEX` ahead of the `ADD CONSTRAINT`s by hand (0007 does, with the reason in its header): same statements, same final shape, so the snapshot still matches and the CI drift check stays green. `pnpm build` and `pnpm test` say nothing about this — only running `node scripts/migrate.mjs` does. The inverse, so the rule is not applied where it does not belong (#29): a foreign key whose REFERENCED column is a PRIMARY KEY has an implicit unique index already, so the generated `ADD COLUMN` → `ADD CONSTRAINT` → `CREATE INDEX` order is safe as written. It bites COMPOSITE keys pointing at a unique index the same migration creates. Two lines to check by hand; guessing costs a stranded deploy. FIRED AGAIN IN #35 (`events_media (event_id, id)`, reproduced in psql against a populated database) and in #36 (`events_rsvp (event_id, id)`, emitted second-to-last). The per-file assertion each of those left behind was a LIST WEARING A CLOSURE'S CLOTHES — it named the pair of strings just fixed — so `test/api-boundary.test.ts` now WALKS every migration, parses each composite `ADD CONSTRAINT … REFERENCES t(a,b)` out of the SQL and requires the matching `CREATE UNIQUE INDEX` earlier in that file or in an earlier one, counting the constraints it found so an empty offender list cannot come from a regex that stopped matching. It does the same for an EXISTING table (`ADD CONSTRAINT … CHECK`, `CREATE UNIQUE INDEX … WHERE …`) without any interactive prompt, index after constraint, so check that order by hand there too. `on delete set null` and a COMPOSITE foreign key are incompatible here: SET NULL nulls every column of the key, including the `event_id` that is NOT NULL, and drizzle-kit does not emit Postgres 15's `SET NULL (column_list)`. So a child that must be event-scoped AND survive its parent's deletion gets `restrict` plus an explicit transaction in the domain (`deletePlaceAsPlanner`), and a single-column reference keeps `set null` (`events_timeline_item.place_id`, where the scoping check is then code and not the database).
- 2026-09-16: a control whose first option is "Unassigned" cannot express a SET — picking a second name takes the thing away from the first. A multiple `USelectMenu` drops that pseudo-value (an empty selection already says it), and the client must then send the DIFFERENCE as adds and removes rather than a "set these people" call, or two planners with the card open silently undo each other's additions.

## What the app actually is (re-checked against the code at fb99a70, #10)

- 2026-09-14: There are **no `ENABLE_*` feature flags** and no `ADMIN_EMAIL` in the source. A capability is on when it is configured: no storage config → uploads unavailable, no mail transport → `/login` withholds the magic link and says why, no `ZAEME_SERVICE_TOKEN` → every `/api/v1` call 401s. Do not add a flag to turn something off; leave it unconfigured.
- 2026-09-14: Object storage reads the generic `S3_*` names with `R2_*` as legacy aliases (`server/utils/storage.ts`). The bucket variable is `R2_BUCKET`, never `R2_BUCKET_NAME`, and `R2_PUBLIC_URL` is read nowhere — public poster art goes through `/api/public/poster/{key}`, re-authorised from the events domain on every hit.
- 2026-09-14: Chat is request/response (`GET`/`POST …/messages` on host, invite and public). No SSE endpoint, no WebSocket, no `replyToId`, no soft delete, no unread indicator — whatever an old roadmap line promises.
- 2026-09-14: Only four Inngest functions exist (`rsvp.confirmed`, `event.published`, `event.reminder`, `event.cancelled`). `travel.recluster`, `sbb.refresh`, `datepoll.closed` and `packList.seed` were never written, and neither was any SBB/transport client, to-do or pack list, or Anthropic call. There is no `/mcp` route: Enterprise generates its tools from the OpenAPI contract.
- 2026-09-14: `events_timeline_item.pollId` is a **dead column**, read nowhere outside the schema file. `events_media.caption` is set at `…/media/confirm` and has no edit route afterwards. A column existing is still not the feature existing.
- 2026-09-14: Timeline reordering is up/down buttons swapping `sortOrder` with two PATCHes (`HostTimelineCard.vue`), not drag-and-drop.
- 2026-09-14: Event types are `hosted | concert | series | trip | party`. `trip` (itinerary + budget) and `party` (a `core` invite wave polls the date, then `general` goes out — `invite.tier`) are easy to miss because the docs listed only the first three until #10.
- 2026-09-15: EDITING `scripts/api-smoke.sh` BY `s.index(anchor)` IS A TRAP — check names repeat across blocks ("a direction that is not up or down" exists in the timeline block AND in a new one), so a splice whose `end` anchor matches an EARLIER block silently duplicates everything between them. The tell is the suite running a section twice, an event slug coming back with a `-2` suffix, and a neighbouring block's assertions failing for no reason. Scope the edit to the section first (slice between its `echo "== … =="` and the next one), then assert `count == 1` on every anchor.
## Rebase and generated files

- 2026-09-14 (#63): `pnpm db:generate` needs no `DATABASE_URL` (it reads the schema files only) and says "No schema changes, nothing to migrate" on a clean tree. Adding a column to `server/database/schema/*.ts` without regenerating leaves `pnpm lint`, `pnpm typecheck` and all 194 tests green — `git status --porcelain -- server/database/migrations` after a generate is the signal, and it names the file drizzle-kit wanted to write. Editing only a JSDoc comment on a schema column generates nothing either, and drizzle's `text('x', { enum: [...] })` renders a plain text column — widening that list is a TypeScript change and not a migration. Both are free ways to keep `server/database/schema/*.ts` honest. AND IT NEEDS A TTY the moment a table both gains and loses a column — drizzle-kit asks whether the new one is a RENAME of a dropped one, and dies with "Interactive prompts require a TTY" under the Bash tool. `script -qec "pnpm db:generate" /dev/null < <fifo>` with a background writer feeding `\r` after a few seconds answers it; the first option in the list is "create column", which is the answer whenever the two columns are unrelated.
- 2026-09-14: `pnpm db:generate` emits `ADD COLUMN <x> integer NOT NULL` with NO default for a `.notNull()` column with no default, which ABORTS on a table that already has rows — and that statement runs as the Kitchen `migrate` task, where a failure strands production on the previous release. Add-with-default → `UPDATE` → `DROP DEFAULT` in the generated file keeps the final shape identical to the snapshot and survives both cases; prove it by replaying 0000..N-1 into a scratch database, inserting a row, and then applying yours. The `UPDATE` is only needed when the backfill comes from ANOTHER COLUMN; when the literal default is the truth for every existing row, add-with-default → `DROP DEFAULT` is the whole migration. When there is NO sensible constant, the shape is different again: add it NULLABLE, backfill, then `ALTER COLUMN … SET NOT NULL`, so a row the backfill missed fails loudly instead of inheriting a placeholder. And DROP the default every time: one left on a NOT NULL column is a silent-wrong-value generator, handing a forgetful insert the default instead of an error. Keep the TS schema free of `.default()` so the generated snapshot agrees. #59 hit the no-constant case for real (`currency`, whose truth differs per row) and took the nullable-then-SET-NOT-NULL shape. A DETERMINISTIC KEY IS NOT A RE-RUNNABLE STATEMENT, either: `md5(a || ':' || b)` as a backfill's primary key buys no idempotence when the `INSERT` has no `ON CONFLICT` — a second run aborts on the PK. It is moot (drizzle records what it applied and wraps the pending set in one transaction), which is exactly why that claim survived into a committed header until a reviewer read it.
- 2026-09-14..16 (merging four): REPLAYING PART OF THE CHAIN IS THE CHEAPEST PROOF A MIGRATION IS SAFE ON A FULL DATABASE, and is worth the minute every time. Trim `meta/_journal.json` to `idx < N` (copy it first and put it back immediately — it is generated, and a half-trimmed journal committed by accident skips a migration on every deploy), migrate a scratch database, INSERT by hand the rows the change is about, restore the journal, migrate again, then read the rows back and try the inserts the new constraint must refuse. It proved 0007's `ADD COLUMN place_id text` keeps an existing item's `location`, that #32's constraint held, and that #36's destructive DROP carried its assignments across — each in three `psql` lines, and each the only evidence the `upgrade` job was going to be green. Replaying the .sql files directly instead needs `psql -1` PER FILE: 0006 builds a `CREATE TEMP TABLE … ON COMMIT DROP` and psql's default statement-per-transaction drops it before the next statement (`relation "_zaeme_entry_61" does not exist`), which drizzle's migrator avoids by wrapping the pending set in one transaction.
- 2026-09-14: a server util that only *calls* `$fetch` inside a function imports cleanly under plain vitest (no Nuxt auto-imports there); one that touches it at module scope does not. Pure helpers can therefore live beside the database code they are used by and still be unit-tested directly.
- 2026-09-14: better-auth's `useSession()` is safe to call on an SSR'd public page (the invite page does), but `data` is null until the client resolves it. Gate any "you need an account" message on `session.value.isPending`, or a signed-in friend sees the refusal for a frame before it is taken back. NOTE the property is BORROWED: it holds because the session atom defaults to pending and fetches only in the browser. `useSession(useFetch)` returns `isPending: false`, and a version bump could seed the session server-side — so never let PRIVACY rest on it, only timing.
- 2026-09-16 (#31): a NULLABLE column with no default and no backfill is the cheapest migration there is — one `ALTER TABLE … ADD COLUMN x text`, a catalogue change that visits no row — and the 42830 constraint-before-index trap in 0007's header does not apply (no foreign key, no unique index). Prove it anyway: trim `meta/_journal.json` to `idx < N`, migrate, INSERT a row, restore the journal, migrate again; the pre-existing row must come out with the column null and every other value untouched, and a re-run must apply 0.
- 2026-09-14: a migration that writes NEW rows into a table whose old NOT NULL columns it is about to drop has to `ALTER COLUMN … DROP NOT NULL` first — the new rows have nothing to put in `name`/`email`. Order is read the old columns, loosen them, write around them, drop them.
- 2026-09-14: a migration statement that AGGREGATES the table it then inserts into must capture the aggregate in a `CREATE TEMP TABLE … ON COMMIT DROP` first; the second and third inserts would otherwise re-read the rows the first one just wrote. `ON COMMIT DROP` is safe because drizzle's migrator wraps the pending set in one transaction.
- 2026-09-14: drizzle supports PARTIAL unique indexes (`uniqueIndex(...).on(...).where(sql`...`)`), and they are how you keep a uniqueness guarantee when the same key legitimately appears twice per parent: `(expense_id, account_id) where amount_cents >= 0` plus the `< 0` twin gives at most one debit and one credit per account per entry. Predicate on the AS-SPENT amount, not the base one — a tiny expense at a small rate converts to 0 base cents and would collide with itself. And Postgres counts two NULLs as DISTINCT by default, so a unique index over nullable columns already permits any number of rows that leave them empty — which is often the rule you wanted and sometimes the one you forgot to write.
- 2026-09-16: a new TABLE gets the same `ci-upgrade-check.mjs` treatment as a new column: snapshot what the PREVIOUS release reported (a sentinel `'no-field'` when it has no such field at all), and assert against THAT rather than against `null`. The same assertion also catches a row that VANISHED, which is the failure a purely additive migration is not supposed to be able to have and is the worst one if it does.
- 2026-09-16: an old PLAIN foreign key replaced by a COMPOSITE one is a migration that can abort on data the app never wrote. `events_media.assigned_rsvp_id` referenced `events_rsvp (id)` with no event scoping, so a backfill into a table keyed on `(event_id, rsvp_id)` must JOIN on both columns — a cross-event row is then left behind instead of stranding production on the previous release. Seeded one by hand and watched it be skipped.
