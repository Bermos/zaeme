# Reviewer memory

Defects that reached `main` in Bermos/zaeme, and the blind spots that let them
through. Each is a question to ask of every diff. Dated, one line each.

The entries dated 2026-09-14 are the seed: they are read out of `CLAUDE.md`, the
commit history and the open issues at the moment this file was created, not out of a
review. Everything after them is earned.

## Defects that shipped

- 2026-09-14: A correlated sub-select in the domain was silently counting nothing (`3e6df6f`), and no test in CI touches a database (#14). Ask: does this query's correctness depend on a real Postgres, and did anything run it against one?
- 2026-09-14: A deploy-time guard keyed on the sha256 of the dump it guarded had to download the artifact in order to decide whether it needed the artifact — the artifact was deleted, the task failed, and the deploy stopped (Enterprise `30c429c`, same platform, same shape). Ask: can this guard short-circuit without fetching the thing it guards, and does it fail safe?
- 2026-09-14: A dry-run log cut the magic link in half (`95c3642`), so the only way into an instance with no mail transport was broken by a logging detail. Ask: is the credential this path emits intact and usable, and is there a test that reads it back?
- 2026-09-14: Timeline `PATCH` was lost in the Enterprise transplant (#8) — a route that existed and then quietly did not. Ask: does the screen that calls this route still have a route to call?
- 2026-09-14: A script's own header documented a sign-in recipe that no longer works (no `Origin` header — better-auth answers 403 — and "the newest `zaeme_verification` row" picks the wrong account); the PR that discovered it fixed its own copy and left the header (#49). Ask: did this change prove a committed instruction wrong, and was the instruction fixed where a human will read it?
- 2026-09-14: `HostTimelineCard.move()` reorders by two sequential PATCHes; a half-completed swap leaves two items on the SAME `sortOrder`, after which ↑/↓ computes a no-op forever and that pair can never be separated again (reproduced on a scratch Postgres; PR #50 copies the pattern to `/admin`, where there is no add/delete to recover with). Ask: is this multi-request write atomic, and what state does its half-failure leave the user in?
- 2026-09-14: `... is distinct from <new value>` in an UPDATE, added to keep `updated_at` still on rows that did not move, also removes those rows from the statement's LOCK set — so a concurrent writer's change to them survives the "atomic" renumber and two rows end up sharing a number anyway (PR #50, reproduced on Postgres 16; without the clause the same interleaving is a clean permutation). Ask: does this WHERE clause skip rows the statement's correctness depends on holding?
- 2026-09-14: A single-statement multi-row renumber deadlocks against itself — one `update … from (cte)` acquires row locks in plan order, and two concurrent copies deadlocked 7 times in 9000 statements over an 8-row list (PR #50). Ask: does this statement lock more than one row, in an order nothing pins?
- 2026-09-14: A write and the read that drives the UI must break ties on the SAME key. `applyTimelineItemMove` orders by `sort_order, starts_at, created_at, id`; every list query stops at `created_at` — so on a tied itinerary (the state the fix exists to repair) the user's first row is the server's last, ↓ is refused as off-the-end and the item silently jumps to the bottom (PR #50, reproduced). Ask: is the order the server computes the order the user was looking at?
- 2026-09-14: A shared guard typed `{ status: string }` throws away the schema's enum union, so any row with a `status` field compiles and a renamed status stops matching with typecheck green (`assertEventOpenToGuests`, #52). Ask: does this extraction widen a parameter's type past what the schema already proves?
- 2026-09-14: Re-inlining an extracted rule at its original call site and running the one boundary test proved a new assertion load-bearing in 40 seconds. Mutation-test structural tests; do not read them.
- 2026-09-15: drizzle-kit emits EVERY constraint before EVERY index, so a composite foreign key whose target `uniqueIndex` is created in the same migration aborts with `42830` on an empty database and a full one alike — a `migrate` task that strands production (#67/0007; reproduced by regenerating the file from the schema, and by hand in psql). `0006` has the same shape and escaped only because its target index happened to be emitted earlier. Ask of every migration adding a composite FK: was the file hand-reordered, and is the reordered statement SET identical to a fresh `pnpm db:generate`?
- 2026-09-15: A COOLDOWN CHECKED ONLY AT RESERVATION TIME DOES NOT REACH THE CALLERS ALREADY IN THE QUEUE — `createRateGate.take()` tests `blockedUntil` before it sleeps, so three concurrent searches all reserve, the first gets a 429, `block(5 min)` fires, and the other two still go out: 3 provider calls where the file's own comment promises 1 (#68, reproduced with an overlapping-sleep fake clock). Ask: does this guard re-check its condition after the await, or only before it?
- 2026-09-15: THE REFUSAL AND THE COOLDOWN IT ARMS REPORT DIFFERENT REASONS — only the request that actually received the 429 answers `geocoder_rate_limited`; every request in the five minutes after is refused by the gate and answers `geocoder_busy`, i.e. "our own queue", which is the opposite diagnosis (#68, reproduced). The PR's degrade table says "429 or 403 (and for five minutes after) -> geocoder_rate_limited". Ask: does the state a guard ENTERS report the same reason as the event that caused it?
- 2026-09-15: A DISCRIMINATED FAILURE REASON THAT NOTHING READS IS NOT A DEGRADE PATH — `GeocodeUnavailable`'s three values reach no log and no screen (the card branches on `status` alone), so "the geocoder is blocking us" and "the geocoder is down" are one sentence to the owner, which is the failure the domain doc names at length (#68). Ask: which code branches on this enum, and would the person who has to act ever see it?
- 2026-09-16: A DERIVED COLUMN THAT IS ALSO A RECORDED FACT IS DESTROYED BY THE RECOMPUTE THAT OWNS IT — #59 treats `amount_base_cents` as derived, but for a `manual` row it is the figure a person typed off their bank statement; `setEventCurrency` overwrites it, so CHF 234.00 -> EUR -> CHF at real (non-inverse) rates comes back CHF 235.18, the original is gone from the database, and the row still reports `fxRateSource: 'manual'` / "checked against a statement" (reproduced: 118 cents on one row, and it compounds per change). Ask: does this write overwrite a value a PERSON supplied, and is that value recoverable anywhere afterwards?
- 2026-09-16: THE ARGUMENT A DIFF MAKES AGAINST ONE PATH CAN BE TRUE OF THE PATH IT SHIPS — #59's own comment refuses per-share conversion on the write path because "the payer would be credited 83.68 for handing over 83.67 and `paidCents` would stop meaning what they paid"; on the recompute path the payer IS credited 10791 for an entry of 10790 (reproduced), and the smoke suite pins 10791 as the expected answer. Ask: does the failure mode this comment rejects reappear on the branch the comment is defending?
- 2026-09-16: AN "IS THIS APPROXIMATE" FLAG KEYED ON THE WRONG PREDICATE — `approximate: expenses.some(e => e.currency !== baseCurrency)` is false for exactly the rows a currency change made most approximate (receipt EUR, trip now EUR, settled figure a chained conversion, residual 0), so the one line the acceptance criterion asks for is absent on the budget that needs it (#59, reproduced). `fxRate !== '1'` is the predicate that means "something was converted". Ask: does this flag test the CAUSE or a proxy that the new code path falsifies?
- 2026-09-16: A UI "did this convert?" TEST THAT NO LONGER DECIDES IT — `isForeign(x) = x.currency !== x.baseCurrency` in `BudgetCard.vue` hides both the settled amount and the rate line on a row where the two are equal but the amounts differ, so after a currency change the card says "Ana paid EUR 245.25" while the balances use EUR 254.25 and nothing on screen explains the gap (#59). There are no component tests, so the API smoke that pins the payload proves nothing about the screen. Ask: does the component's predicate for "converted" still agree with the server's?
- 2026-09-16: A FORM THAT RESTATES WHAT IT PREFILLED DEFEATS THE SERVER RULE THAT REFUSES TO GUESS — `BudgetCard.startEdit` fills a mixed `even` split with NO per-person values and `saveExpense` ALWAYS sends `splitMode` + `participants`, so correcting only the TITLE of an even split with one amount pinned re-splits it evenly (4000/3000/3000 -> 3334/3333/3333, executed) and `resplitFromRecord`'s 422 is unreachable from the only surface a person uses (#72/#27). Ask: does the client send the field whose ABSENCE the server rule depends on?
- 2026-09-16: A PREFILLED OVERRIDE SENT BACK IS READ AS A NEW ASSERTION — the same form prefills `fxRate` from the row's frozen rate and resends it on every save, so every foreign expense recorded OR corrected through the product is written `fxRateSource: 'manual'` with a fabricated `statedAmountCents`, and both of `resolveEditConversion`'s "nobody said anything" branches are dead code from the product (#72; zaeme#71 is the write-path half). Ask: does this form distinguish "the user typed it" from "we showed it to them"?

## Blind spots

- 2026-09-14: CI runs lint, typecheck and vitest and nothing else — no database, no server, no browser. `pnpm smoke:api` and `pnpm smoke:passkey` are the only things that execute the API and the passkey ceremony, and a human has to run them. Ask: which of the two does this PR need, and did it run?
- 2026-09-14: The contract test is a bijection over paths and methods only. Ask: is a field removed, a type narrowed, a status code changed — anything breaking for Enterprise's generated tools — while every test stays green?
- 2026-09-14: `test/api-boundary.test.ts` asserts where code lives and what it imports. Ask: is the gate this handler calls the right gate for its credential, which the test cannot see?
- 2026-09-14: Nothing notices when Enterprise's vendored copy of the spec drifts from this one (#11). Ask: does this PR oblige a follow-up in the other repository, and is it raised?
- 2026-09-14: The events domain has almost no coverage (#9). Ask: what would fail if this change were reverted in place, and is it a test?
- 2026-09-14: Merging deploys. Ask of every diff: what happens on the way up — a migration that cannot run twice, a startup that now needs an unset variable, a task that fails on the state the instance is really in?
- 2026-09-14: A field can be stored, typed and surfaced in the UI while no code consumes it — `events_expense.currency` shipped that way and produced a confidently wrong balance. Ask: does anything actually READ this column, and does the read do what the schema comment claims?
- 2026-09-14: `/api/v1` additions are the owner's call, not a refactor's (#8). Ask of any diff touching `docs/zaeme-api.openapi.yaml`: was exposing this to Enterprise's generated tools decided, or did it arrive as a side effect of building a feature for the guest surface?
- 2026-09-14: A smoke suite that SKIPS on unset env and still exits 0 can go green having proved a third less — `scripts/api-smoke.sh` is 106 checks with `S3_*` and both session cookies set and 78 without, and the `api` job asserts no count (#49). Ask: does the job fail when a GROUP skips, or only when a check fails?
- 2026-09-14: The `api` job migrates an EMPTY database once; the deploy runs the same `migrate` task against a database already at the previous release, every time (#49). Ask: does anything prove this migration applies to the state production is really in, and that a second run is a no-op?
- 2026-09-14: Anything a step writes to `$GITHUB_ENV` is reprinted in every later step's env group, so a session cookie minted there is in the build log unless `::add-mask::` went out first (#49). Ask: is the credential this job mints masked, or merely "not echoed"?
- 2026-09-14: The `api` job boots `.output/server/index.mjs` beside the full dev `node_modules` and with `INNGEST_EVENT_KEY` unset, so a runtime dependency declared as a devDependency still boots green and the whole event fan-out is never sent (#49). Ask: what does the built artifact resolve and reach in production that CI leaves standing beside it?
- 2026-09-14: A test that greps source proves a route EXISTS, not that it works: swapping the two same-typed `string` arguments of `applyTimelineItemUpdate(eventId, itemId)` keeps all 101 tests and `pnpm typecheck` green while every host timeline PATCH 404s (#50). Ask: does this diff thread two interchangeable ids through a new extraction, and does anything execute them?
- 2026-09-14: `server/domain/index.ts` re-exports the unguarded `*AsOwner` and `apply*` writes, and `test/api-boundary.test.ts` polices only `requireOwner`'s import site — a future host route could import an owner-scoped write and skip the gate with nothing failing. Ask: does this PR add an authorisation-free domain export, and is anything stopping a non-admin handler from calling it?
- 2026-09-14: A comment in the vendored contract can outlive the gap it describes — `docs/zaeme-api.openapi.yaml` still calls timeline editing a "KNOWN GAP, carried across the boundary unchanged" after #8 closed it on both human surfaces, and Enterprise reads that file. Ask: does closing this issue falsify a committed comment somebody else reads?
- 2026-09-14: Moving a write off the invite capability URL silently drops every check that rode on `resolveInviteToken` — revoked, expired, usage limit, draft/cancelled event (#52: a participant can still record money on a cancelled event through a revoked link's event). Ask of any route that changes credential: which checks belonged to the OLD credential, and does the new gate repeat them?
- 2026-09-14: A field added to a domain view reaches every surface that returns that view unshaped — `loadBudget`'s new `addedByEmail` went out over the invite capability URL because the guest budget is `loadBudget` verbatim, while `/api/v1` was safe only because `v1-shapes.ts` picks fields explicitly. Ask: which credentials can see this new field, not just the one it was built for?
- 2026-09-14: A new authorization primitive can be WIDER than the old one on the same operation — `assertParticipant` passes any planner role, so a `logistics` planner may write an expense on `/api/me` that `addExpenseAsPlanner` refuses them on `/api/host` and `/api/v1`. Ask: does the new gate agree with the existing gate on the same verb?
- 2026-09-14: A `v-if` on the invite page is part of the read surface. Dropping `complete` put the trip's expenses, balances and settlement in the SSR HTML of a generic link, for a visitor who has typed nothing. Ask of any condition removed on `app/pages/i/[token].vue`: who can now see this without identifying themselves?
- 2026-09-14: Smoke-run claims are checkable arithmetic: `grep -cE '^\s*(check|contains) ' scripts/api-smoke.sh` minus the assertions inside each `if [ -n "${…COOKIE:-}" ]`/`S3_BUCKET` block says which groups ran. #52's "102 passed" reconciled exactly (83 unconditional + 14 owner-cookie + 5 guest-cookie, S3 skipped) while its prose "nine new checks" was eight. Reconcile the total; the adjectives prove nothing.
- 2026-09-14: A structural boundary test is worth mutation-testing, not reading: add the forbidden import to a real handler, run the one file, revert. #52's new block caught all three injected violations in under a minute.
- 2026-09-14: An honesty check that falls back to the optimistic value fails open — `emailConfigured ?? true` on a failed `/api/setup/status` fetch promises a magic link on a dry-run instance. Ask of every "can this instance actually do it" question: which way does it answer when the question itself fails?
- 2026-09-14: #49's Postgres-backed `api` job does not close #14 by itself — a raw-SQL domain write is still proved by nothing unless a smoke check calls it. On PR #50, dropping the transposition, unscoping the renumber from one event to EVERY event, and hard-wiring the direction each left `pnpm test` (105) and eslint green, because the new tests grep for `row_number()` and count the word `update`. Ask: does anything EXECUTE this SQL, and would it fail if the SQL did the opposite?
- 2026-09-14: Hiding private data behind a `v-if` keyed on a client-resolved session hides it from the SSR MARKUP only — `useFetch` serialises the whole response into `__NUXT__` regardless, and the element reappears the instant the session resolves. Ask: does "not in the server HTML" mean "nobody sees it", or only "the crawler and the chat unfurler do not"?
- 2026-09-14: SSR privacy resting on a library's initial state is unpinned — better-auth's session atom is `isPending: true` until the browser fetches, and `useSession(useFetch)` returns `isPending: false`. A dependency bump or a one-word call-site change flips it and no test says so. Ask: which third-party default is this condition silently relying on?
- 2026-09-14: A smoke script's fire-and-forget teardown can be restoring nothing — `completed` has no outgoing transitions, so #52's status reset 409s into `/dev/null` and leaves the fixture in the wrong state for whatever is appended next. Ask: is every `> /dev/null` request in the suite actually legal, or just unchecked?
- 2026-09-14: A smoke `contains` over a FILTERED read can be satisfied by the unfiltered part of the same body — `/api/admin/audit` returns `{entries, summary}` and the summary carries `"surface":"me"` and `"actorKind":"participant"` whatever the query asked for, so #56's owner check passed with `entries: []` after every `me` row was relabelled (reproduced). Ask of every needle: does it appear anywhere OUTSIDE the part the filter controls?
- 2026-09-14: A polling helper is only as good as its needle. `audit_await` waits for a string the unfiltered summary already contains, so it returns on the first read and the write-then-read race it exists to close stays open for the assertion that follows (#56). Ask: would this poll ever loop?
- 2026-09-14: The label the edge computes and the answer the handler computes for the SAME request can disagree — a `logistics` planner who also RSVP'd is `participant` to `assertParticipant` and `planner` to the audit, because the recorder asks "is there a planner row" and the gate asks "what standing is this" (#56, reproduced on Postgres). Ask: does the middleware's version of the question match the handler's?
- 2026-09-14: A colour map keyed by an enum with `?? 'primary'` silently gives the NEXT value added the same badge as the one it was introduced to be told apart from; #56's boundary test pins the filter list and the select items but not the colours. Ask: does the test that says "every kind is covered" cover every place the kind is rendered?
- 2026-09-14: The comment the issue QUOTES as the admission of the bug survived the fix in place — `events_expense`'s table doc still says "`currency` is informative — balances are only computed within one currency" three lines above the columns that make both halves false (#57). Ask: did the diff delete the sentence that described the old behaviour, or only the behaviour?
- 2026-09-14: A smoke check whose success value equals the default proves nothing about the write — #57's only successful `PATCH /api/admin/settings` sets `CHF` on an instance already on `CHF` and then asserts `"configured":true`, so a domain that discarded the input and wrote `DEFAULT_BASE_CURRENCY` passes CI whole. Ask: does the happy-path assertion distinguish the value the caller SENT from the value that was already there?
- 2026-09-14: Suite ORDER can make an acceptance criterion untestable — the settings block runs after the fixture records expenses, and the 409 invariant then forbids the very change the criterion asks for, so "the owner can change the base currency" has no green check anywhere (#57). Ask: is this assertion placed where the state still permits the thing it claims to prove?
- 2026-09-14: A `catch { return null }` around an outbound call makes the failure path indistinguishable from a broken implementation — #57's only automated FX check asserts 422 for a currency nobody quotes, which is what a wrong URL, a renamed field or an undefined `$fetch` would also produce. Ask: would this check still pass if the thing it exercises did not exist?
- 2026-09-14: An invariant enforced in one write path and relied on by a read path needs the two in the same transaction — #57 reads the instance base before `addExpense`'s tx opens and counts disagreeing rows outside `setInstanceBaseCurrency`'s upsert, so an owner changing the base on an empty instance concurrently with a first write leaves a row the budget then mislabels. Ask: what serialises the check against the write it is checking?
- 2026-09-14: A NOT NULL column with a PERMANENT default fails silently where one without a default fails loudly — #57 drops the default on `amount_base_cents` but keeps `DEFAULT 'CHF'` / `DEFAULT '1'` beside it, so an insert that sets the amount and forgets the pair is a confidently wrong row. Ask: for each new column, does forgetting it abort or does it invent an answer?
- 2026-09-14: A refusal that protects an invariant needs an in-product escape hatch — #57's 409 locks the instance base currency the moment any expense exists, and `/admin` has no way to delete one, so an owner who is not a planner of the trip is out of options short of SQL. Ask: which state does this guard make permanent, and what does the owner do then?
- 2026-09-14: A feature's executed coverage can all sit on the credential that is easiest to script. #60's 29 new smoke checks post only to `/api/v1`; breaking `/api/me`'s `splitMode` enum and `weight` regex — the exact route `BudgetCard` calls from the invite and host pages — left `pnpm test` (167), eslint, `nuxt typecheck` AND the 217-check smoke floor green while every percentage split from the product would 400 (reproduced). Ask: is the new field exercised on the credential the PRODUCT uses, or only on the service token?
- 2026-09-14: There are no component tests, so arithmetic duplicated into a `.vue` is executed by nothing — #60 reimplements the server's 4-decimal scale (`scaleEntered`/`unscale`/a hard-coded `1_000_000` for 100%) in `BudgetCard.vue` to drive the running total. The copies agree today; a change to `WEIGHT_SCALE` or `FULL_PERCENT` moves one of them and the form then refuses a valid split or permits an invalid one, silently. Ask: does this diff put a server rule in a second place, and does anything run the copy?
- 2026-09-14: A `numeric(n,s)` column SILENTLY ROUNDS an over-precise value (`33.33339` → `33.3334`) and only errors past `10^(n-s)` (Postgres 16, reproduced), so a boundary regex whose digit counts exactly match the column is the only thing keeping stored intent equal to entered intent. Ask of each new numeric column: does the API refuse what the column would round, and is that refusal executed over HTTP or only unit-tested in the domain?
- 2026-09-14: drizzle's pg migrator compares each file's `folderMillis` against the SINGLE latest applied `created_at` (`pg-core/dialect.cjs`), so a migration whose journal `when` is earlier than one already applied is skipped — never applied, never reported, deploy green. Ask of two concurrent branches each adding a migration: will the one that merges second regenerate, or will its file be quietly skipped?
- 2026-09-14: An enum that claims to record INTENT only records the intents it names — #60's `even` still honours explicit per-person amounts, so an `even` row and an `exact` row can be identical in the shares, and `split_mode='even'` says nothing about which participants were explicit. The committed comments promising an edit can "re-split without re-typing" are already false for that case. Ask: can two values of this enum describe the same stored rows, and does a comment claim otherwise?
- 2026-09-14: An acceptance criterion can be met by MANUFACTURING the condition it names — #62 switched share conversion from group apportionment to per-share so the `Rounding` account would have a line to receive, breaking "shares sum to the expense total in base" to stop `Rounding` being vacuous. Restoring apportionment left 181/184 vitest green (only the three asserting the drift itself failed) and reproduced all six changed smoke needles verbatim. Ask: did this diff CREATE the condition the criterion describes, or find it?
- 2026-09-14: A property test outlives the path it guarded and stays green proving nothing — #62 keeps #25's 6x40x12 sweep asserting `apportionCents` sums to the converted total while `buildEntryLines` no longer calls it for conversion at all, so 2880 cases now exercise a call the write path does not make. Ask: is the function this sweep exercises still on the path the feature ships?
- 2026-09-14: A whole new domain file can arrive with zero unit tests while the suite grows — `server/domain/accounts.ts` (425 lines) has none, and neutering `resolveCategoryAccount` (a PURE function needing no database) so every line posts to `Uncategorised` left all 184 vitest green, exactly as the implementer's own mutation table reported. Ask: which new functions take no database, and does any test call them?
- 2026-09-14: Rows inserted by one statement share `now()` to the microsecond, so a read ordered `created_at, id` over cuid2 ids returns an ARBITRARY permutation (reproduced) — #62 documents `Expense.lines` as "in the order they were written" and never returns that order, and `shares[]` silently stopped following the participant list. Ask: does this ordering claim survive a tie on the first key?
- 2026-09-14: An unknown-key FALLBACK in a read decides what a row counts as — `loadBudget` reads accounts, then expenses, then lines in three unsynchronised statements and types any line whose account it did not see as `accountKind: 'category'`, so a member account committed between statement one and three is added to `totalCents` and vanishes from the balances. Ask: what does the fallback COUNT AS, and does the read hold one snapshot?
- 2026-09-14: A two-refusal rule reads in the order the code checks, not the order the product means — #62 answers 409 "still has expenses posted to it" when `Uncategorised` holds lines, implying emptying it would allow deletion, and only 422 "part of every budget" once it is empty. Ask: does the first refusal a user can reach describe a way out that does not exist?
- 2026-09-14: Two FKs on one row need not agree — `events_expense_share.account_id` and `.event_id` are independent, so a line can name another event's account (reproduced on Postgres 16); only a composite FK or a check ties them. Ask: do two foreign keys on this row have to point at the same parent, and what enforces it?
- 2026-09-14: An assertion that RECOMPUTES the server's own arithmetic from the server's own response can never go red — `ci-upgrade-check.mjs`'s "the trip total is the sum of its category debits" reimplements `computeTotalCents` over the same `lines[]` the server derived it from, so it counts toward the run's pass total while being unable to fail (#64). Ask: does this check have an independent source of truth, or does it restate the answer it is checking?
- 2026-09-14: TWO FLOORS IN ONE SCRIPT CAN DISAGREE — #64's `snapshot` passes on 2 expenses (the canary's own) while `verify` demands 24 assertions and the canary alone produces 20, so a future smoke suite that leaves nothing behind fails with "this run proved almost nothing" having checked everything it could. Ask: are the minimum the producer accepts and the minimum the consumer demands the same number?
- 2026-09-14: A two-build CI job that rejects a committed fixture still carries one — #64's canary speaks `POST /api/v1/events{,/expenses}` as the PREVIOUS release accepts it and reads `lines[]`/`accountKind` as the NEW one answers it, so a breaking `/api/v1` change fails `upgrade` at the snapshot step until that script is edited. The PR body's "nothing has to be kept in sync" is overstated; it fails loudly, which is the part that matters. Ask: which contract does the checker itself speak, and whose release is it speaking for?
- 2026-09-14: The upgrade job's post-migration reader only sees what `GET /api/v1/events` returns for the SERVICE caller and only events holding expenses, so money the guest account wrote through `/api/me`, and every non-money row a migration touches (timeline, RSVP, invite, audit), is proved only by the migration not ABORTING on it — not by anything reading it back (#64). Ask: does "rows the previous release wrote" mean all of them, or the ones one credential can list?
- 2026-09-14: `push` + `pull_request` under one concurrency group leaves THREE cancelled check runs on the head SHA beside the green ones and puts the PR in `mergeable_state: unstable`; harmless until a required check or auto-merge is switched on, and it looks like flaky CI to anyone reading the commit list (#64). Ask: what do the duplicate triggers leave behind on the commit, not just in the runner minutes?
- 2026-09-15: A CLIENT MERGE CAN OVERRIDE THE SERVER'S SORT KEY. `mergeItinerary` orders timed legs by `departsAt` before `sort_order`, so `applyItineraryLegMove`'s renumbering — one statement, `for update`, transposition, all of #8's machinery — changes nothing on the invite page for any leg that has a departure time, while `server/domain/places.ts` comments that the two orders agree "down to the last tiebreak" (#67, reproduced with a two-leg fixture). Ask: does the screen that shows the order use the same FIRST key the reorder writes?
- 2026-09-15: A smoke needle can be satisfied by a row the OTHER credential wrote — #67's "the guest's own leg is in the SSR payload" is `"mode":"walk","departsAt":null`, which the host's own untimed walk leg in the same fixture matches. Ask of every needle labelled with a writer: is it unique to that writer, or does the fixture contain a twin?
- 2026-09-15: A two-column "both or neither" rule enforced only in the domain leaves a row the product cannot repair: a half-coordinate `events_place` prefills the host edit form and then 422s on every save, including a rename (#67; the DB accepts it, there is no CHECK anywhere in the chain). Ask: what enforces this pair in the database, and what does the edit screen do with a row that already breaks it?
- 2026-09-15: A doc comment can cite a test file that was never written — `app/utils/itinerary-order.ts` says `test/itinerary-order.test.ts` calls it; the tests live in `test/places-and-legs.test.ts` (#67). Ask: does every path a new comment names exist?
- 2026-09-15: The prose beside `MIN_SMOKE_CHECKS` is not checked by the floor it explains — #67's comment says "adds 48" beside a 278 -> 329 bump and a diff of 51 assertions. Ask: does the comment in the file that says "Keep this EXACT" reconcile with its own arithmetic?
- 2026-09-15: A new domain view returned UNSHAPED over the invite URL widens the read surface invisibly: `PlaceView.note`/`address`/`lat`/`lng` reach every link holder through `getInvitePage`, and no guest screen renders them — so the host who typed the note has no way to discover it is public (#67; #42's shape with the discoverability half added). Ask: which new fields are in the guest PAYLOAD but on no guest SCREEN?
- 2026-09-15: CI CAN MAKE LIVE THIRD-PARTY CALLS WITHOUT ANY CHECK DEPENDING ON THEM — #68's smoke block sends ~6 real Nominatim requests per run (queries carry `$SUFFIX` so they can never be cache hits), in both the `api` and `upgrade` jobs, from shared GitHub runner IPs, under the product's own `zaeme/1.0 (+...)` User-Agent whose contact URL in CI is `http://127.0.0.1:3111`. "No check depends on the geocoder answering" is a different claim from "no request is sent"; a UA-pattern block earned by CI lands on production. Ask: does this suite TALK to a third party, not just depend on one?
- 2026-09-15: A UNIQUENESS SELF-EXCLUSION ON AN UPDATE IS PROVED BY NOTHING UNLESS A TEST RE-SENDS THE ROW'S OWN KEY — replacing `rows.find(r => r.id !== exceptPlaceId)` with `rows[0]` in `assertOsmRefFree` left all 273 vitest green AND every new smoke check green, because the only PATCH that carries an OSM ref carries ANOTHER place's (#68). Ask: does any test edit a row while re-sending the very value the uniqueness check is about?
- 2026-09-15: A COLUMN THE SERVER NOW WRITES CAN BE WRITE-ONLY FROM THE PRODUCT — #68's search sets `events_place.osm_type`/`osm_id`, the host card's `Place` interface omits them, `savePlace` never sends them and nothing clears them, so a geocoded place's coordinates can be hand-corrected while it still claims an OSM feature, and the documented escape hatch ("name the second pin by hand") is unreachable once a suggestion has been clicked. Ask: can the UI that WRITES this field also clear and correct it?
- 2026-09-16: THE `upgrade` JOB CANNOT SEE A BACKFILL WHOSE TWO CANDIDATE SOURCES AGREE ON EVERY ROW CI CAN PRODUCE — #59's migration fills `events_event.currency` from each trip's expenses, and the three new `ci-upgrade-check.mjs` assertions pass byte-identically if it fills from the instance setting instead, because the previous release's `addExpense` always wrote `base_currency = instanceBaseCurrency()` and its 409 forbade the two diverging (reproduced end to end on Postgres 16: 13 events, 29 expenses, zero differing rows). One of the three (`baseCurrency` untouched) guards a column this migration never writes. Ask: can the fixture this job builds actually DISTINGUISH the right backfill from the wrong one, or only from a wrong constant?
- 2026-09-16: DELETED SMOKE CHECKS RECONCILE AS ARITHMETIC TOO — `comm -23` over sorted labels counts a RENAME as a deletion; #59's "six removed" was five removals plus one strengthened rename, and 373 -> 429 raw assertion lines matched the 378 -> 434 floor exactly. Check each removed label against the code that used to satisfy it before believing either number. Ask: for each deleted check, is the behaviour gone, or only the sentence?
- 2026-09-16: A SPEC SENTENCE CAN BE FALSIFIED BY THE SAME PR THAT EDITS THE PARAGRAPH AROUND IT — `docs/zaeme-api.openapi.yaml:2267` still promises `fxRate` is `"1"` whenever `currency` is already `baseCurrency`, and #59's recompute produces exactly that row with `fxRate` 1.0366972477 (the schema comment at `events.ts:655` repeats the claim). The contract test is a bijection over paths and methods and sees none of it; Enterprise generates MCP tools from the document. Ask: does any invariant the spec still ASSERTS survive the new write path?
- 2026-09-16: A MUTATION TABLE IS A CLAIM ABOUT CI, NOT ABOUT A HAND-BUILT FIXTURE — #59's "caught only by the `upgrade` job" was verified against a trimmed-journal database the implementer constructed by hand, on a state CI cannot reach. Re-run the mutation against the fixture the JOB builds before accepting the row. Ask: is the state that makes this mutation visible reachable through the previous release's own API?
- 2026-09-16: AN UPGRADE-JOB ASSERTION CAN ENCODE A PROPERTY OF WHICHEVER RELEASE IS THE BASE — `ci-upgrade-check.mjs`'s "its rate is not claimed to have been checked" was trivially true while the base predated `fx_rate_source`, and went red with 12 failures on the first branch cut after #59 landed, whatever its diff (#72). Ask of a red job on an unrelated-looking PR: is the failure about this diff, or about the base moving under a constant?
- 2026-09-16: 300 LINES OF NEW DOMAIN CAN CARRY ZERO VITEST — `grep -rn 'updateExpense\|resolveEditConversion' test/` is empty on #72; only the pure `resplitFromRecord` is unit-tested, so the category rule, the evidence rule, the gate and both human zod schemas are proved by the smoke suite alone. Ask: grep each new exported name in `test/` and say which appear.
- 2026-09-16: AN OVERSTATED FIXTURE SENTENCE TRAVELS FROM THE PR BODY INTO COMMITTED FILES — "3/2/1 of 100.01 is a different number on every person" is false (the middle share is 3334 either way) and is repeated in `.github/workflows/ci.yml` and `scripts/api-smoke.sh`; only the test file's own comment is accurate. Evaluate the fixture, do not read its prose.
- 2026-09-16: THE WORKTREE CAN BE AHEAD OF THE PR — #72's fix for its own red `upgrade` job existed only as an uncommitted change in the worktree, invisible on the head SHA. Ask: does `git status --porcelain` in the checkout agree with what CI ran?
