# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# npm lock file (project uses pnpm)
package-lock.json

# Agent worktrees. The Claude Code harness checks the repo out again under here
# when a subagent runs in worktree isolation, so this is a whole second copy of
# the tree — never repo content, and committing it would be committing the repo
# into itself.
.claude/worktrees/
